// @ts-nocheck
import { createClient } from "@supabase/supabase-js";
import postgres from "postgres";
const DB=Deno.env.get("SUPABASE_DB_URL"),SUPA_URL=Deno.env.get("SUPABASE_URL");if(!DB||!SUPA_URL)throw new Error("runtime vars missing");const sql=postgres(DB,{prepare:false,max:1}),BUCKET="rona-portal-private",PDF_MAX=50*1024*1024,UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
function runtimeKey(kind){const legacy=kind==="pub"?Deno.env.get("SUPABASE_ANON_KEY"):Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(legacy)return legacy;const raw=Deno.env.get(kind==="pub"?"SUPABASE_PUBLISHABLE_KEYS":"SUPABASE_SECRET_KEYS");if(raw){const parsed=JSON.parse(raw);if(parsed.default)return parsed.default}throw new Error("key missing")}
const service=createClient(SUPA_URL,runtimeKey("secret"),{auth:{persistSession:false,autoRefreshToken:false}});function send(status,body){return new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}})}function claims(token){try{const p=token.split(".")[1].replace(/-/g,"+").replace(/_/g,"/");return JSON.parse(atob(p+"=".repeat((4-p.length%4)%4)))}catch{return{}}}
async function adminContext(req){const authorization=req.headers.get("authorization");if(!authorization?.startsWith("Bearer "))return null;const token=authorization.slice(7),client=createClient(SUPA_URL,runtimeKey("pub"),{auth:{persistSession:false,autoRefreshToken:false},global:{headers:{Authorization:authorization}}}),{data,error}=await client.auth.getUser(token);if(error||!data.user)return null;const sid=claims(token).session_id;if(typeof sid!=="string"||!UUID_RE.test(sid))return null;const rows=await sql`select a.portal_user_id,a.display_name,a.roles,s.not_after from portal_private.resolve_portal_auth(${data.user.id}::uuid,${sid}) a join auth.sessions s on s.id=${sid}::uuid and s.user_id=${data.user.id}::uuid where a.session_allowed and (s.not_after is null or s.not_after>now())`;if(rows.length!==1||!(rows[0].roles||[]).map(String).includes("ADMIN"))return null;return{auth:String(data.user.id),user:String(rows[0].portal_user_id),name:String(rows[0].display_name||""),sid}}
function requestIds(req){const rh=req.headers.get("x-request-id"),ch=req.headers.get("x-correlation-id");return{requestId:rh&&UUID_RE.test(rh)?rh:crypto.randomUUID(),correlationId:ch&&UUID_RE.test(ch)?ch:null}}async function audit(tx,ctx,action,entityType,entityId,req,metadata={}){const{requestId,correlationId}=requestIds(req);await tx`insert into portal_private.audit_events(actor_user_id,actor_role,action,entity_type,entity_id,request_id,correlation_id,metadata) values(${ctx.user}::uuid,'ADMIN',${action},${entityType},${entityId},${requestId}::uuid,${correlationId}::uuid,${sql.json(metadata)})`}
function safeFilename(name){const base=String(name||"signed-contract.pdf").split(/[\\/]/).pop()||"signed-contract.pdf",cleaned=base.replace(/[^A-Za-z0-9._-]+/g,"_").replace(/^_+|_+$/g,"").slice(0,120);return cleaned.toLowerCase().endsWith(".pdf")?cleaned:`${cleaned||"signed-contract"}.pdf`}async function sha256Hex(bytes){const digest=await crypto.subtle.digest("SHA-256",bytes);return Array.from(new Uint8Array(digest)).map(b=>b.toString(16).padStart(2,"0")).join("")}
async function parseSignedPdf(req){let form;try{form=await req.formData()}catch{throw Object.assign(new Error("INVALID_MULTIPART"),{status:400})}const file=form.get("file");if(!(file instanceof File))throw Object.assign(new Error("PDF_REQUIRED"),{status:400});if(file.size<=0||file.size>PDF_MAX)throw Object.assign(new Error("PDF_SIZE_INVALID"),{status:400});if(!/\.pdf$/i.test(file.name||"")||(file.type&&file.type!=="application/pdf"))throw Object.assign(new Error("PDF_TYPE_INVALID"),{status:400});const bytes=await file.arrayBuffer();if(new TextDecoder().decode(bytes.slice(0,5))!=="%PDF-")throw Object.assign(new Error("PDF_SIGNATURE_INVALID"),{status:400});const clientId=String(form.get("clientId")||"").trim(),claimsSigned=String(form.get("adminClaimsBilateralSigned")||"").toLowerCase()==="true";let attestation={};try{attestation=JSON.parse(String(form.get("adminAttestation")||"{}"))}catch{throw Object.assign(new Error("ATTESTATION_INVALID"),{status:400})}if(!claimsSigned||attestation?.confirmed!==true||String(attestation?.type||"")!=="BILATERAL_SIGNED_CONTRACT_ATTESTATION")throw Object.assign(new Error("BILATERAL_ATTESTATION_REQUIRED"),{status:400});return{file,bytes,clientId,sha256:await sha256Hex(bytes),attestation}}
async function contractCandidate(contractId,clientId){const rows=await sql`select ct.id contract_key,ct.contract_id,ct.contract_status,ct.current_external_contract_number,ct.authority_state::text contract_authority,ct.lifecycle_state::text contract_lifecycle,ct.current_signed_document_id,ct.signed_contract_confirmed_at,ct.signed_contract_confirmed_by,cl.id client_key,cl.client_id,cl.legal_name,cl.authority_state::text client_authority,cl.lifecycle_state::text client_lifecycle from portal_private.contracts ct join portal_private.clients cl on cl.id=ct.client_key where ct.contract_id=${contractId} and (${clientId}='' or cl.client_id=${clientId}) limit 1`;if(!rows.length)throw Object.assign(new Error("CONTRACT_CANDIDATE_NOT_FOUND"),{status:404});const row=rows[0];if(["ARCHIVED","SUPERSEDED"].includes(String(row.contract_lifecycle))||String(row.contract_authority)==="REJECTED")throw Object.assign(new Error("CONTRACT_CANDIDATE_NOT_ACTIVATABLE"),{status:409});if(["ARCHIVED","SUPERSEDED"].includes(String(row.client_lifecycle))||String(row.client_authority)==="REJECTED")throw Object.assign(new Error("CLIENT_CANDIDATE_NOT_ACTIVATABLE"),{status:409});return row}
async function findContractDocument(contract){const rows=await sql`select d.id,d.document_id,d.current_version_id from portal_private.documents d where d.contract_key=${contract.contract_key}::uuid and d.client_key=${contract.client_key}::uuid and upper(d.document_type) in ('КОНТРАКТ','SIGNED_BILATERAL_CONTRACT') order by (d.id=${contract.current_signed_document_id}::uuid) desc nulls last,(d.document_id=${contract.contract_id}) desc,d.updated_at desc limit 1`;return rows[0]||null}async function verifiedCurrentSigned(contract){if(!contract.current_signed_document_id||!contract.signed_contract_confirmed_at||!contract.signed_contract_confirmed_by)return null;const rows=await sql`select d.document_id from portal_private.documents d join portal_private.document_versions dv on dv.id=d.current_version_id and dv.document_key=d.id join portal_private.storage_objects so on so.document_version_key=dv.id and so.storage_state='VERIFIED' where d.id=${contract.current_signed_document_id}::uuid and d.authority_state='CONFIRMED'::portal_private.authority_state_enum and d.lifecycle_state='ACTIVE'::portal_private.lifecycle_state_enum and dv.authority_state='CONFIRMED'::portal_private.authority_state_enum and dv.lifecycle_state='ACTIVE'::portal_private.lifecycle_state_enum and dv.is_current and dv.is_effective limit 1`;return rows[0]||null}
async function rawStorageObjectId(objectName){const rows=await sql`select id from storage.objects where bucket_id=${BUCKET} and name=${objectName} limit 1`;return rows[0]?.id?String(rows[0].id):null}async function uploadRawPdf(contract,parsed){const objectName=`contracts/${contract.client_id}/${contract.contract_id}/${crypto.randomUUID()}-${safeFilename(parsed.file.name)}`,{error}=await service.storage.from(BUCKET).upload(objectName,new Uint8Array(parsed.bytes),{contentType:"application/pdf",upsert:false,cacheControl:"3600"});if(error)throw Object.assign(new Error("STORAGE_UPLOAD_FAILED"),{status:502});const rawId=await rawStorageObjectId(objectName);if(!rawId){try{await service.storage.from(BUCKET).remove([objectName])}catch{}throw Object.assign(new Error("STORAGE_OBJECT_ID_MISSING"),{status:502})}return{objectName,rawId}}async function cleanupRaw(objectName){try{await service.storage.from(BUCKET).remove([objectName])}catch{}}
async function activateSignedContract(ctx,req,contractId){const parsed=await parseSignedPdf(req),contract=await contractCandidate(contractId,parsed.clientId);if(await verifiedCurrentSigned(contract))throw Object.assign(new Error("SIGNED_PDF_ALREADY_REGISTERED"),{status:409});const raw=await uploadRawPdf(contract,parsed);try{const result=await sql.begin(async tx=>{let document=await findContractDocument(contract),documentKey,documentId;if(!document){documentKey=crypto.randomUUID();documentId=String(contract.contract_id);const conflict=await tx`select 1 from portal_private.documents where document_id=${documentId} limit 1`;if(conflict.length)documentId=`${contract.contract_id}-SIGNED-${crypto.randomUUID().slice(0,8)}`;await tx`insert into portal_private.documents(id,document_id,document_type,client_key,contract_key,authoritative_filename,source_system,source_version,source_timestamp,authority_state,lifecycle_state) values(${documentKey}::uuid,${documentId},'SIGNED_BILATERAL_CONTRACT',${contract.client_key}::uuid,${contract.contract_key}::uuid,${parsed.file.name},'ADMIN_PORTAL','SIGNED_CONTRACT_ACTIVATION_V2',now(),'CONFIRMED'::portal_private.authority_state_enum,'ACTIVE'::portal_private.lifecycle_state_enum)`}else{documentKey=String(document.id);documentId=String(document.document_id)}const maxRows=await tx`select coalesce(max(version_number),0) max_version from portal_private.document_versions where document_key=${documentKey}::uuid`,nextVersion=Number(maxRows[0]?.max_version||0)+1,previous=await tx`select id from portal_private.document_versions where document_key=${documentKey}::uuid and is_current order by version_number desc limit 1`,versionKey=crypto.randomUUID();await tx`insert into portal_private.document_versions(id,document_key,version_number,authoritative_filename,sha256,storage_path,uploaded_by,is_current,is_effective,source_system,source_version,source_timestamp,authority_state,lifecycle_state) values(${versionKey}::uuid,${documentKey}::uuid,${nextVersion},${parsed.file.name},${parsed.sha256},${raw.objectName},${ctx.user}::uuid,false,false,'ADMIN_PORTAL',${`SIGNED_CONTRACT_ACTIVATION_V${nextVersion}`},now(),'CONFIRMED'::portal_private.authority_state_enum,'ACTIVE'::portal_private.lifecycle_state_enum)`;if(previous.length){await tx`update portal_private.document_versions set is_current=false,is_effective=false,superseded_by=${versionKey}::uuid,authority_state='SUPERSEDED'::portal_private.authority_state_enum,lifecycle_state='SUPERSEDED'::portal_private.lifecycle_state_enum where id=${previous[0].id}::uuid`;await tx`update portal_private.storage_objects set storage_state='SUPERSEDED',updated_at=now() where document_version_key=${previous[0].id}::uuid and storage_state='VERIFIED'`}await tx`update portal_private.document_versions set is_current=true,is_effective=true where id=${versionKey}::uuid`;await tx`insert into portal_private.storage_objects(bucket_id,object_name,storage_object_id,object_kind,client_key,contract_key,document_version_key,content_type,byte_size,sha256,storage_state,created_by,verified_by,verified_at) values(${BUCKET},${raw.objectName},${raw.rawId}::uuid,'DOCUMENT',${contract.client_key}::uuid,${contract.contract_key}::uuid,${versionKey}::uuid,'application/pdf',${parsed.file.size},${parsed.sha256},'VERIFIED',${ctx.user}::uuid,${ctx.user}::uuid,now())`;await tx`update portal_private.documents set document_type='SIGNED_BILATERAL_CONTRACT',authoritative_filename=${parsed.file.name},current_version_id=${versionKey}::uuid,authority_state='CONFIRMED'::portal_private.authority_state_enum,lifecycle_state='ACTIVE'::portal_private.lifecycle_state_enum,updated_at=now() where id=${documentKey}::uuid`;await tx`update portal_private.clients set authority_state='CONFIRMED'::portal_private.authority_state_enum,lifecycle_state='ACTIVE'::portal_private.lifecycle_state_enum,updated_at=now() where id=${contract.client_key}::uuid`;await tx`update portal_private.contracts set contract_status='ACTIVE',authority_state='CONFIRMED'::portal_private.authority_state_enum,lifecycle_state='ACTIVE'::portal_private.lifecycle_state_enum,current_signed_document_id=${documentKey}::uuid,signed_contract_confirmed_at=now(),signed_contract_confirmed_by=${ctx.user}::uuid,updated_at=now() where id=${contract.contract_key}::uuid`;const activated=await tx`update portal_private.client_user_bindings b set status='ACTIVE'::portal_private.binding_status_enum,authority_state='CONFIRMED'::portal_private.authority_state_enum,lifecycle_state='ACTIVE'::portal_private.lifecycle_state_enum,reason='Admin Portal: signed contract confirmed; pending company access activated',updated_at=now() from portal_private.portal_users u where b.contract_key=${contract.contract_key}::uuid and b.client_key=${contract.client_key}::uuid and b.user_id=u.id and b.status='PENDING'::portal_private.binding_status_enum and b.revoked_at is null and u.status='ACTIVE'::portal_private.portal_user_status_enum returning b.id`;await audit(tx,ctx,"ADMIN_CLIENT_AUTHORITY_CONFIRMED","CLIENT",String(contract.client_id),req,{contract_id:String(contract.contract_id)});await audit(tx,ctx,"ADMIN_CONTRACT_AUTHORITY_CONFIRMED","CONTRACT",String(contract.contract_id),req,{client_id:String(contract.client_id),document_id:documentId});await audit(tx,ctx,"BILATERAL_CONTRACT_ATTACH_CONFIRMED","CONTRACT",String(contract.contract_id),req,{document_id:documentId,sha256:parsed.sha256,storage_verified:true});await audit(tx,ctx,"SIGNED_CONTRACT_STORAGE_REGISTERED","DOCUMENT",documentId,req,{contract_id:String(contract.contract_id),client_id:String(contract.client_id),sha256:parsed.sha256,object_name:raw.objectName,attestation_type:parsed.attestation.type,client_download_allowed: true});if(activated.length)await audit(tx,ctx,"PENDING_CLIENT_BINDINGS_ACTIVATED","CONTRACT",String(contract.contract_id),req,{count:activated.length,binding_ids:activated.map(x=>String(x.id))});return{documentId,version:nextVersion,activatedBindings:activated.length}});return{clientId:String(contract.client_id),contractId:String(contract.contract_id),documentId:result.documentId,version:String(result.version),status:"ACTIVE",originalFilename:String(parsed.file.name),storageKey:raw.objectName,storageObjectId:raw.rawId,mimeType:"application/pdf",sizeBytes:Number(parsed.file.size),sha256:parsed.sha256,uploadedByUserId:String(ctx.user),uploadedAtServer:new Date().toISOString(),source:"ADMIN_PORTAL",isCurrent:true,serverConfirmed:true,clientDownloadAllowed:true,externalNumber:contract.current_external_contract_number?String(contract.current_external_contract_number):String(contract.contract_id),contractDate:null,bilateralSignedConfirmed:true,signatureStatus:"BILATERALLY_SIGNED",adminAttestation:{type:"BILATERAL_SIGNED_CONTRACT_ATTESTATION",confirmed:true},activatedPendingBindings:result.activatedBindings}}catch(e){await cleanupRaw(raw.objectName);throw e}}
Deno.serve(async req=>{if(req.method!=="POST")return send(405,{ok:false,code:"METHOD_NOT_ALLOWED"});const ctx=await adminContext(req);if(!ctx)return send(403,{ok:false,code:"ADMIN_SESSION_REQUIRED"});const path=new URL(req.url).pathname,match=path.match(/\/rona-admin-contract-activation\/contracts\/([^/]+)\/signed-document\/attach$/);if(!match)return send(404,{ok:false,code:"ROUTE_NOT_FOUND"});try{return send(200,{ok:true,document:await activateSignedContract(ctx,req,decodeURIComponent(match[1]))})}catch(e){console.error("rona-admin-contract-activation error",e);const status=Number(e?.status||500);return send(status>=400&&status<600?status:500,{ok:false,code:String(e?.message||"SERVER_ERROR")})}});
