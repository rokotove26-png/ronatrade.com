import { createClient } from "@supabase/supabase-js";
import postgres from "postgres";
export const DB=Deno.env.get("SUPABASE_DB_URL");export const SUPA_URL=Deno.env.get("SUPABASE_URL");if(!DB||!SUPA_URL)throw new Error("runtime vars missing");export const sql=postgres(DB,{prepare:false,max:1});export const origins=new Set(["https://ronaoil.com","https://www.ronaoil.com"]);export const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;export type Ctx={auth:string;user:string;name:string;roles:string[];sid:string;exp:string|null};
export function runtimeKey(kind:"pub"|"secret"){const legacy=kind==="pub"?Deno.env.get("SUPABASE_ANON_KEY"):Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(legacy)return legacy;const raw=Deno.env.get(kind==="pub"?"SUPABASE_PUBLISHABLE_KEYS":"SUPABASE_SECRET_KEYS");if(raw){const parsed=JSON.parse(raw);if(parsed.default)return parsed.default}throw new Error("key missing")}
export function headers(origin:string|null){const out:Record<string,string>={"content-type":"application/json; charset=utf-8","cache-control":"no-store","vary":"Origin","access-control-allow-headers":"authorization, content-type, x-request-id, x-correlation-id, x-idempotency-key","access-control-allow-methods":"GET, POST, OPTIONS"};if(origin&&origins.has(origin))out["access-control-allow-origin"]=origin;return out}export function send(origin:string|null,status:number,body:unknown){return new Response(JSON.stringify(body,(_k,value)=>typeof value==="bigint"?value.toString():value),{status,headers:headers(origin)})}export function apiRoute(url:URL){const marker="/rona-portal-api",i=url.pathname.indexOf(marker);return i>=0?(url.pathname.slice(i+marker.length)||"/"):url.pathname}function claims(token:string){try{const p=token.split(".")[1].replace(/-/g,"+").replace(/_/g,"/");return JSON.parse(atob(p+"=".repeat((4-p.length%4)%4)))}catch{return{}}}
export async function authenticate(req:Request):Promise<Ctx|null>{const authorization=req.headers.get("authorization");if(!authorization?.startsWith("Bearer "))return null;const token=authorization.slice(7),client=createClient(SUPA_URL!,runtimeKey("pub"),{auth:{persistSession:false,autoRefreshToken:false},global:{headers:{Authorization:authorization}}}),{data,error}=await client.auth.getUser(token);if(error||!data.user)return null;const sid=claims(token).session_id;if(typeof sid!=="string"||!uuid.test(sid))return null;const rows=await sql`select a.portal_user_id,a.display_name,a.roles,s.not_after from portal_private.resolve_portal_auth(${data.user.id}::uuid,${sid}) a join auth.sessions s on s.id=${sid}::uuid and s.user_id=${data.user.id}::uuid where a.session_allowed and (s.not_after is null or s.not_after>now())`;if(rows.length!==1)return null;return{auth:data.user.id,user:String(rows[0].portal_user_id),name:String(rows[0].display_name||""),roles:(rows[0].roles||[]).map(String),sid,exp:rows[0].not_after?new Date(rows[0].not_after).toISOString():null}}
export async function sessionScope(c:Ctx){const clients=new Set<string>(),contracts=new Set<string>(),deals=new Set<string>(),persons=new Set<string>(),legalEntities=new Set<string>();if(c.roles.includes("ADMIN")||c.roles.includes("RONA_OPERATOR")){for(const r of await sql`select cl.client_id,ct.contract_id,d.deal_id from portal_private.clients cl join portal_private.contracts ct on ct.client_key=cl.id left join portal_private.deals d on d.client_key=cl.id and d.contract_key=ct.id`){clients.add(String(r.client_id));contracts.add(String(r.contract_id));if(r.deal_id)deals.add(String(r.deal_id))}}if(c.roles.includes("CLIENT")){for(const r of await sql`select distinct cl.client_id,ct.contract_id,d.deal_id from portal_private.client_user_bindings b join portal_private.clients cl on cl.id=b.client_key join portal_private.contracts ct on ct.id=b.contract_key left join portal_private.deals d on d.client_key=b.client_key and d.contract_key=b.contract_key and portal_private.client_user_has_deal_access(${c.user}::uuid,d.id,now()) where b.user_id=${c.user}::uuid and portal_private.client_user_has_contract_access(${c.user}::uuid,b.contract_key,now())`){clients.add(String(r.client_id));contracts.add(String(r.contract_id));if(r.deal_id)deals.add(String(r.deal_id))}}if(c.roles.includes("AGENT")){for(const r of await sql`select distinct ap.agent_person_id,ale.agent_legal_entity_id,cl.client_id,ct.contract_id,d.deal_id from portal_private.agent_user_bindings ub join portal_private.agent_persons ap on ap.id=ub.agent_person_key join portal_private.agent_legal_entities ale on ale.id=ub.agent_legal_entity_key join portal_private.agent_client_assignments a on a.agent_person_key=ub.agent_person_key and a.agent_legal_entity_key=ub.agent_legal_entity_key join portal_private.agent_deal_terms t on t.assignment_id=a.id join portal_private.deals d on d.id=t.deal_key join portal_private.clients cl on cl.id=d.client_key join portal_private.contracts ct on ct.id=d.contract_key where ub.user_id=${c.user}::uuid and portal_private.agent_user_has_deal_view_access(${c.user}::uuid,d.id,now())`){persons.add(String(r.agent_person_id));legalEntities.add(String(r.agent_legal_entity_id));clients.add(String(r.client_id));contracts.add(String(r.contract_id));deals.add(String(r.deal_id))}}return{client_ids:[...clients],contract_ids:[...contracts],deal_ids:[...deals],agent_person_ids:[...persons],agent_legal_entity_ids:[...legalEntities]}}
export async function recordApiEvent(req:Request,route:string,result:string,status:number,c?:Ctx|null,metadata:Record<string,unknown>={}){const requestHeader=req.headers.get("x-request-id"),correlationHeader=req.headers.get("x-correlation-id"),requestId=requestHeader&&uuid.test(requestHeader)?requestHeader:crypto.randomUUID(),correlationId=correlationHeader&&uuid.test(correlationHeader)?correlationHeader:null;try{await sql`insert into portal_private.portal_api_request_events(request_id,auth_user_id,portal_user_id,method,route,result,http_status,correlation_id,user_agent,metadata) values(${requestId}::uuid,${c?.auth??null}::uuid,${c?.user??null}::uuid,${req.method},${route},${result},${status},${correlationId}::uuid,${req.headers.get("user-agent")},${sql.json(metadata)})`}catch(error){console.error("portal api audit write failed",error)}return requestId}
