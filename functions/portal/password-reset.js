const SUPABASE_URL = 'https://sxawrwzeobaqwwmlkzws.supabase.co';
const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_W2MxTx00ILiugSyZKp8uyQ_zBzcyorL';
const OWNER_ID = 'c4a167ae-cd4f-4296-8f13-ef09ced41968';
const OWNER_EMAIL = 'office_kg@ronaoil.com';
const ACCESS_COOKIE = 'rona_portal_at';
const RESET_COOKIE = 'rona_pwreset';
const SECURITY = Object.freeze({
  'cache-control':'no-store, no-cache, must-revalidate','pragma':'no-cache','referrer-policy':'no-referrer',
  'x-content-type-options':'nosniff','x-frame-options':'DENY','permissions-policy':'camera=(), microphone=(), geolocation=(), payment=()',
  'cross-origin-opener-policy':'same-origin','cross-origin-resource-policy':'same-origin',
  'content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src 'self'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'"
});
function cookies(header) { const out={}; for(const part of String(header||'').split(';')){const i=part.indexOf('=');if(i>0)out[part.slice(0,i).trim()]=part.slice(i+1).trim();} return out; }
function redirect(location) { const h=new Headers(SECURITY);h.set('location',location);return new Response(null,{status:303,headers:h}); }
function html(body,status=200) { const h=new Headers(SECURITY);h.set('content-type','text/html; charset=utf-8');return new Response(body,{status,headers:h}); }
function page(message='') {
  const error=message?`<div class="err" role="alert">${message.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}</div>`:'';
  return `<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>RONA Trade — Новый пароль</title><style>:root{font-family:Inter,Arial,sans-serif;color:#eef4f7;background:#05090d}*{box-sizing:border-box}body{margin:0;min-height:100vh;display:grid;place-items:center;background:radial-gradient(circle at 80% 10%,#152633 0,#071018 42%,#05090d 75%)}.box{width:min(440px,calc(100vw - 32px));padding:28px;border:1px solid rgba(171,220,239,.24);border-radius:18px;background:rgba(8,15,22,.92)}h1{font-size:25px;margin:0 0 6px}.sub{color:#9db1bc;margin:0 0 22px}.field{display:grid;gap:7px;margin:14px 0}.field label{font-size:13px;color:#afc0c9}.field input{width:100%;padding:12px;border-radius:10px;border:1px solid rgba(171,220,239,.26);background:#09121a;color:#fff;font:inherit}.btn{width:100%;margin-top:10px;padding:12px;border:1px solid rgba(224,66,75,.45);border-radius:10px;background:rgba(224,66,75,.15);color:#fff;font-weight:800;cursor:pointer}.btn:disabled{cursor:wait;opacity:.72}.note{font-size:12px;color:#8298a4;line-height:1.45}.err,.ok{padding:10px 12px;border-radius:9px;margin:12px 0;font-size:13px;line-height:1.4}.err{background:#4b1e23;color:#ffdfe3}.ok{border:1px solid #365464;color:#d7e7ee}</style></head><body><main class="box"><h1>Установите новый пароль</h1><p class="sub">RONA Trade — защищённый контур владельца</p>${error}<div id="state" class="ok" style="display:none" aria-live="polite"></div><form id="resetForm" method="post" action="/portal/auth/password-reset" autocomplete="off"><div class="field"><label for="p1">Новый пароль</label><input id="p1" name="password" type="password" minlength="14" maxlength="72" autocomplete="new-password" required></div><div class="field"><label for="p2">Повторите пароль</label><input id="p2" name="confirm" type="password" minlength="14" maxlength="72" autocomplete="new-password" required></div><button id="submitButton" class="btn" type="submit">Сохранить новый пароль</button></form><p class="note">Минимум 14 символов: латинская заглавная и строчная буквы, цифра и специальный символ. После сохранения текущая recovery-сессия будет завершена; вход выполняется заново.</p></main><script>(()=>{'use strict';const form=document.getElementById('resetForm'),p1=document.getElementById('p1'),p2=document.getElementById('p2'),button=document.getElementById('submitButton'),state=document.getElementById('state');const show=(text,bad=false)=>{state.textContent=text;state.style.display='block';state.className=bad?'err':'ok'};const strong=p=>p.length>=14&&p.length<=72&&/[a-z]/.test(p)&&/[A-Z]/.test(p)&&/[0-9]/.test(p)&&/[^A-Za-z0-9]/.test(p);const messages={PASSWORD_POLICY_FAILED:'Пароль не соответствует требованиям: минимум 14 символов, латинские A–Z и a–z, цифра и специальный символ.',PASSWORD_CHANGE_SESSION_REQUIRED:'Recovery-сессия истекла. Вернитесь к восстановлению доступа и получите новый одноразовый код.',OWNER_PASSWORD_CHANGE_MISMATCH:'Recovery-сессия больше не действительна. Получите новый одноразовый код.',ORIGIN_DENIED:'Не удалось подтвердить защищённый запрос. Обновите страницу и повторите.',PASSWORD_GATE_UNAVAILABLE:'Сервис смены пароля временно недоступен. Повторите попытку.',PASSWORD_UPDATE_FAILED:'Пароль не был сохранён. Повторите попытку.'};form.addEventListener('submit',async e=>{e.preventDefault();const p=p1.value,q=p2.value;if(p!==q){show('Пароли не совпадают.',true);return}if(!strong(p)){show(messages.PASSWORD_POLICY_FAILED,true);return}button.disabled=true;button.textContent='Сохраняем…';show('Проверяем recovery-сессию и сохраняем новый пароль…');try{const r=await fetch('/portal/auth/password-reset',{method:'POST',credentials:'same-origin',cache:'no-store',referrerPolicy:'no-referrer',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify({password:p,confirm:q})});const j=await r.json().catch(()=>({}));if(!r.ok||!j?.ok||!j?.redirect){const code=String(j?.code||'PASSWORD_UPDATE_FAILED');show(messages[code]||'Пароль не был сохранён. Повторите попытку.',true);return}p1.value='';p2.value='';show('Пароль сохранён. Выполняется безопасный переход…');location.replace(j.redirect)}catch(_){show('Соединение было прервано. Страница сохранена: повторите нажатие «Сохранить новый пароль». Если ошибка повторится, сообщите мне — код повторно вводить не нужно.',true)}finally{button.disabled=false;button.textContent='Сохранить новый пароль'}})})();</script></body></html>`;
}
async function currentUser(token) {
  if (!token) return null;
  try { const r=await fetch(`${SUPABASE_URL}/auth/v1/user`,{headers:{apikey:SUPABASE_PUBLISHABLE_KEY,authorization:`Bearer ${token}`,accept:'application/json'}}); if(!r.ok)return null; return await r.json(); } catch { return null; }
}
export async function onRequestGet({request}) {
  const c=cookies(request.headers.get('cookie'));
  if(c[RESET_COOKIE]!=='1')return redirect('/portal/login?recovery=required');
  const user=await currentUser(c[ACCESS_COOKIE]);
  if(!user||String(user.id||'')!==OWNER_ID||String(user.email||'').toLowerCase()!==OWNER_EMAIL)return redirect('/portal/login?recovery=expired');
  const url=new URL(request.url);const code=String(url.searchParams.get('error')||'');const messages={policy:'Пароль не соответствует требованиям.',session:'Recovery-сессия истекла. Получите новый одноразовый код.',service:'Сервис смены пароля временно недоступен. Повторите попытку.'};
  return html(page(messages[code]||''));
}
