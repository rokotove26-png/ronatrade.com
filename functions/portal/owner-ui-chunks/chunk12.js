export default "(()=>{'use strict';\nif(window.__RONA_VISUAL_V2_DEDUP_TITLES__)return;\nwindow.__RONA_VISUAL_V2_DEDUP_TITLES__=true;\nconst targets=[['claims','Претензии'],['access','Клиенты и агенты']];\nconst norm=v=>String(v||'').replace(/\\s+/g,' ').trim();\nfunction isCandidate(el,title){\n  if(!el||el.closest('.rona-visual-hero'))return false;\n  if(el.closest('nav,button,a,[role=button],.breadcrumbs,.breadcrumb'))return false;\n  if(norm(el.textContent)!==title)return false;\n  for(const c of el.children||[])if(norm(c.textContent)===title)return false;\n  const tag=String(el.tagName||'').toUpperCase();\n  const cs=getComputedStyle(el),fs=parseFloat(cs.fontSize)||0,fw=parseInt(cs.fontWeight,10)||0;\n  return /^H[1-6]$/.test(tag)||el.getAttribute('role')==='heading'||fs>=18||fw>=700;\n}\nfunction hideDuplicateTitles(){\n  for(const [id,title] of targets){\n    const root=document.getElementById('page-'+id);\n    if(!root)continue;\n    const hero=root.querySelector(':scope > .rona-visual-hero');\n    if(!hero)continue;\n    const candidates=Array.from(root.querySelectorAll('*')).filter(el=>isCandidate(el,title));\n    const duplicate=candidates.find(el=>!el.dataset.ronaVisualDuplicateTitle);\n    if(!duplicate)continue;\n    duplicate.dataset.ronaVisualDuplicateTitle='hidden';\n    duplicate.setAttribute('aria-hidden','true');\n    duplicate.style.setProperty('display','none','important');\n  }\n}\nlet queued=false;\nconst schedule=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;hideDuplicateTitles()})};\nif(document.readyState==='loading')document.addEventListener('DOMContentLoaded',schedule,{once:true});else schedule();\nnew MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});\nsetInterval(schedule,1500);\n})();";
