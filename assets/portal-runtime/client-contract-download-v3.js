(()=>{'use strict';
const MARK='20260829-client-contract-v3-authoritative-projection-v4';
if(window.__RONA_CLIENT_CONTRACT_DOWNLOAD_V3__===MARK)return;
window.__RONA_CLIENT_CONTRACT_DOWNLOAD_V3__=MARK;
window.__RONA_CLIENT_CONTRACT_DOWNLOAD_V2__=MARK;
window.__RONA_CLIENT_CONTRACT_DOWNLOAD_V1__=MARK;
const API='/portal/api',REFRESH_MS=30000,STYLE_ID='ronaClientContractDownloadV3Style';
const state={entries:new Map(),loading:false,lastLoad:0,renderTimer:0};
const norm=v=>String(v??'').replace(/\s+/g,' ').trim(),low=v=>norm(v).toLocaleLowerCase('ru-RU');
const GENERIC=new Set(['общество','ограниченной','ответственностью','совместное','предприятие','company','limited','liability','joint','venture','contract','контракт','rona','trade','ооо','осоо','сп','с','llc']);
const APOSTROPHES="'’‘`´ʼ";
async function request(path){const r=await fetch(API+path,{credentials:'same-origin',cache:'no-store',headers:{accept:'application/json'}});const b=await r.json().catch(()=>null);if(!r.ok||b?.ok===false)throw new Error(String(b?.code||b?.error?.code||('HTTP_'+r.status)));return b}
function visible(el){if(!el||!el.isConnected)return false;const s=getComputedStyle(el);if(s.display==='none'||s.visibility==='hidden'||Number(s.opacity)===0)return false;const r=el.getBoundingClientRect();return r.width>0&&r.height>0}
function tokenKey(v){return low(v).replace(new RegExp('['+APOSTROPHES+']','g'),'').replace(/[^a-zа-яё0-9]+/gi,'')}
function legalWords(name){return (norm(name).match(/[A-Za-zА-Яа-яЁё0-9]+(?:['’‘`´ʼ-][A-Za-zА-Яа-яЁё0-9]+)*/g)||[]).filter(Boolean)}
function canonicalBusinessWords(ctx){const words=legalWords(ctx?.legal_name);while(words.length&&GENERIC.has(low(words[0])))words.shift();return words}
function identityTokens(ctx){return canonicalBusinessWords(ctx).map(tokenKey).filter(x=>x.length>=3&&!GENERIC.has(x)).filter((x,i,a)=>a.indexOf(x)===i)}
function currentContractDocument(documents){const docs=Array.isArray(documents)?documents:[];const type=d=>String(d?.document_type||'').trim().toUpperCase();return docs.find(d=>type(d)==='SIGNED_CONTRACT')||docs.find(d=>type(d)==='SIGNED_BILATERAL_CONTRACT')||docs.find(d=>type(d)==='КОНТРАКТ')||docs.find(d=>type(d)==='CONTRACT')||docs.find(d=>type(d).includes('CONTRACT')||type(d).includes('КОНТРАКТ'))||null}
async function mapLimit(items,limit,worker){const q=[...items],out=[];const runners=Array.from({length:Math.min(limit,q.length)},async()=>{while(q.length){const item=q.shift();if(item===undefined)break;out.push(await worker(item))}});await Promise.all(runners);return out}
function effectiveContext(base,detail){const dc=detail?.contract||{},number=norm(dc.current_external_contract_number)||norm(base.current_external_contract_number)||null;return{...base,...dc,current_external_contract_number:number}}
async function refresh(force=false){if(state.loading)return;if(!force&&Date.now()-state.lastLoad<REFRESH_MS){render();return}state.loading=true;try{const boot=await request('/v1/client/bootstrap'),contexts=Array.isArray(boot?.data?.contexts)?boot.data.contexts:[];const rows=await mapLimit(contexts,4,async base=>{try{const detail=await request('/v1/client/context?clientId='+encodeURIComponent(base.client_id)+'&contractId='+encodeURIComponent(base.contract_id));const data=detail?.data||{};return{context:effectiveContext(base,data),document:currentContractDocument(data.documents)}}catch(error){console.error('RONA contract context',base?.contract_id,error);return{context:base,document:null}}});state.entries=new Map(rows.map((r,i)=>[String(r.context?.contract_id||r.context?.client_id||i),r]));state.lastLoad=Date.now();window.__RONA_CLIENT_CONTRACT_DOWNLOAD_STATE__={version:MARK,entries:rows.map(r=>({client_id:r.context?.client_id||null,legal_name:r.context?.legal_name||null,contract_id:r.context?.contract_id||null,current_external_contract_number:r.context?.current_external_contract_number||null,contract_status:r.context?.contract_status||null,document_id:r.document?.document_id||null,storage_object_id:r.document?.storage_object_id||null})),loadedAt:new Date().toISOString()};render()}catch(error){console.error('RONA contract bootstrap',error)}finally{state.loading=false}}
function ensureStyle(){if(document.getElementById(STYLE_ID))return;const s=document.createElement('style');s.id=STYLE_ID;s.textContent=`button[data-rona-contract-download-v3]{appearance:none!important;display:inline-flex!important;align-items:center!important;justify-content:center!important;gap:7px!important;min-height:30px!important;padding:6px 11px!important;border:1px solid rgba(230,190,82,.38)!important;border-radius:999px!important;background:linear-gradient(180deg,rgba(230,190,82,.13),rgba(230,190,82,.07))!important;color:#ecd27e!important;font-family:inherit!important;font-size:10.5px!important;line-height:1.15!important;font-weight:820!important;letter-spacing:.02em!important;white-space:nowrap!important;cursor:pointer!important;box-shadow:inset 0 1px 0 rgba(255,255,255,.04)!important}button[data-rona-contract-download-v3]::before{content:'↓';font-size:13px;font-weight:900;color:#67d9fb}@media(hover:hover) and (pointer:fine){button[data-rona-contract-download-v3]:hover{border-color:rgba(101,217,255,.55)!important;background:rgba(101,217,255,.11)!important;color:#ddf9ff!important}}button[data-rona-contract-download-v3][disabled]{cursor:wait!important;opacity:.72!important}[data-rona-current-company-status]{background:transparent!important;border-color:transparent!important;box-shadow:none!important;cursor:default!important;pointer-events:none!important;transform:none!important}`;document.head.appendChild(s)}
function resolveEntry(node){const scopes=[];let n=node;for(let i=0;n&&n!==document.body&&i<8;i++,n=n.parentElement)scopes.push(low(n.textContent));let best=null,bestScore=0,nextScore=0;for(const entry of state.entries.values()){const c=entry.context||{},contract=low(c.contract_id),client=low(c.client_id),number=low(c.current_external_contract_number),tokens=identityTokens(c);let score=0;for(const s of scopes){if(contract&&s.includes(contract))score=Math.max(score,100);if(client&&s.includes(client))score=Math.max(score,80);if(number&&s.includes(number))score=Math.max(score,70);let tokenScore=0;for(const t of tokens)if(tokenKey(s).includes(t))tokenScore+=3;score=Math.max(score,tokenScore)}if(score>bestScore){nextScore=bestScore;bestScore=score;best=entry}else if(score>nextScore)nextScore=score}return bestScore>=3&&bestScore>nextScore?best:null}
function leafByText(root,predicate){return [...root.querySelectorAll('button,a,span,small,strong,p,div')].find(e=>e.childElementCount===0&&predicate(low(e.textContent)))||null}
function companyCardCandidates(){return [...document.querySelectorAll('article,section,li,div')].filter(visible)}
function findCompanyCard(ctx){const contractKey=low(ctx?.contract_id),clientKey=low(ctx?.client_id),tokens=identityTokens(ctx),c=[];for(const node of companyCardCandidates()){const t=low(node.textContent);if(!t||t.length>9000||(!t.includes('подписанный контракт')&&!t.includes('текущая компания')&&!t.includes('открыть компанию')&&!t.includes('переключиться')))continue;const compact=tokenKey(t),hc=contractKey&&t.includes(contractKey),hi=clientKey&&t.includes(clientKey);let tokenScore=0;for(const x of tokens)if(compact.includes(x))tokenScore+=40;if(!hc&&!hi&&tokenScore<40)continue;let score=(hc?10000:0)+(hi?3000:0)+tokenScore+500-Math.min(t.length,8000)/8;c.push({node,score,len:t.length})}c.sort((a,b)=>b.score-a.score||a.len-b.len);return c[0]?.node||null}
function currentEntryFromDom(){for(const el of document.querySelectorAll('button,a,span,small,strong,p,div')){if(!visible(el)||el.childElementCount!==0||low(el.textContent)!=='текущая компания')continue;const entry=resolveEntry(el);if(entry)return entry}return null}
function numberEntryFor(node){const direct=resolveEntry(node);if(direct)return direct;const r=node?.getBoundingClientRect?.();if(r&&r.top>=0&&r.top<180)return currentEntryFromDom();return null}
function syncContractNumberLabels(){const root=document.body;if(!root)return;const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT),pending=[];let node;while((node=walker.nextNode())){const v=norm(node.nodeValue);if(/номер\s+уточняется/i.test(v)||/контракт\s*(?:№|номер)?\s*уточняется/i.test(v))pending.push(node)}for(const t of pending){const parent=t.parentElement;if(!parent||!visible(parent))continue;const entry=numberEntryFor(parent),num=norm(entry?.context?.current_external_contract_number);if(!num)continue;const before=String(t.nodeValue||'');let after=before.replace(/контракт\s*(?:№|номер)?\s*уточняется/ig,'Контракт № '+num);after=after.replace(/номер\s+уточняется/ig,num);if(after!==before){t.nodeValue=after;parent.dataset.ronaContractNumberSynced='true'}}}
function escapeRe(v){return String(v).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}
function flexibleCanonicalWordRe(word){let out='';for(const ch of String(word)){if(APOSTROPHES.includes(ch))out+='['+escapeRe(APOSTROPHES)+']?';else out+=escapeRe(ch)}return new RegExp('\\b'+out+'\\b','giu')}
function syncCompanyNameSpelling(){for(const entry of state.entries.values()){const ctx=entry.context||{},words=canonicalBusinessWords(ctx);if(!words.length)continue;const canonical=words[0],key=tokenKey(canonical);if(key.length<4)continue;const variants=[canonical.replace(new RegExp('['+APOSTROPHES+']','g'),'')];const card=findCompanyCard(ctx),scopes=[];if(card)scopes.push(card);for(const el of document.querySelectorAll('header,[class*="top"],[class*="context"],[class*="company"]')){if(!visible(el))continue;const resolved=resolveEntry(el);if(resolved===entry&&!scopes.includes(el))scopes.push(el)}for(const scope of scopes){const walker=document.createTreeWalker(scope,NodeFilter.SHOW_TEXT);let t;while((t=walker.nextNode())){const parent=t.parentElement;if(!parent||!visible(parent))continue;let value=String(t.nodeValue||''),next=value;for(const raw of variants){if(tokenKey(raw)!==key)continue;next=next.replace(new RegExp('\\b'+escapeRe(raw)+'\\b','giu'),canonical)}next=next.replace(flexibleCanonicalWordRe(canonical),canonical);if(next!==value)t.nodeValue=next}}}}
function currentStatusNode(card){return leafByText(card,t=>t==='текущая компания')}
function switchNode(card){return leafByText(card,t=>t==='открыть компанию'||t==='переключиться')}
function syncCompanyControls(){ensureStyle();for(const entry of state.entries.values()){const card=findCompanyCard(entry.context);if(!card)continue;const current=currentStatusNode(card);if(current){const control=current.closest('button,a,[role="button"]')||current;control.dataset.ronaCurrentCompanyStatus='true';control.setAttribute('aria-current','true');control.setAttribute('aria-disabled','true');control.setAttribute('role','status');if('disabled' in control)control.disabled=true;if(control.matches('a'))control.removeAttribute('href');control.tabIndex=-1;control.addEventListener('click',preventCurrentCompanyAction,true)}else{const action=switchNode(card);if(action){action.textContent='Переключиться';const control=action.closest('button,a,[role="button"]')||action;control.setAttribute('aria-label','Переключиться на '+norm(entry.context?.legal_name||entry.context?.client_id))}}}}
function preventCurrentCompanyAction(e){e.preventDefault();e.stopImmediatePropagation()}
const unavailableNode=card=>leafByText(card,t=>t.includes('контракт пока недоступен для скачивания')||t==='контракт недоступен для скачивания'||t.includes('файл подписанного контракта не опубликован в кабинете'));
const contractAnchor=card=>leafByText(card,t=>t==='подписанный контракт');
function downloadName(entry,issued){return norm(issued?.object?.filename||entry?.document?.authoritative_filename||'Договор.pdf')||'Договор.pdf'}
function withDownloadDisposition(url,filename){try{const u=new URL(String(url));u.searchParams.set('download',filename);return u.toString()}catch{return String(url||'')}}
async function beginDownload(entry,b){if(b.disabled||!entry?.document?.storage_object_id)return;const idle='Скачать договор PDF';b.disabled=true;b.textContent='Подготовка PDF…';try{const issued=await request('/v1/client/storage/'+encodeURIComponent(entry.document.storage_object_id)+'/signed-url'),url=issued?.signed_url||issued?.data?.signed_url;if(!url)throw new Error('SIGNED_URL_MISSING');const filename=downloadName(entry,issued),a=document.createElement('a');a.href=withDownloadDisposition(url,filename);a.target='_blank';a.rel='noopener';a.download=filename;a.style.display='none';document.body.appendChild(a);a.click();a.remove();b.textContent='Договор открыт'}catch(error){console.error('RONA contract download',error);b.textContent='Не удалось скачать';b.title='Не удалось получить защищённую ссылку. Повторите попытку.'}finally{setTimeout(()=>{if(b.isConnected){b.disabled=false;b.textContent=idle}},1200)}}
function makeButton(entry,template){ensureStyle();const b=document.createElement('button');b.type='button';if(template&&typeof template.className==='string')b.className=template.className;b.dataset.ronaContractDownloadV3=String(entry.context?.contract_id||'');b.dataset.storageObjectId=String(entry.document?.storage_object_id||'');b.textContent='Скачать договор PDF';b.title=norm(entry.document?.authoritative_filename)||'Скачать действующий подписанный договор';b.setAttribute('aria-label','Скачать подписанный договор '+norm(entry.context?.current_external_contract_number||entry.context?.contract_id));b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();beginDownload(entry,b)},true);return b}
function renderEntry(entry){const card=findCompanyCard(entry.context);if(!card)return false;card.dataset.ronaClientContractId=String(entry.context?.contract_id||'');card.dataset.ronaClientId=String(entry.context?.client_id||'');for(const old of card.querySelectorAll('button[data-rona-contract-download],button[data-rona-contract-download-v2]'))old.remove();const existing=card.querySelector('button[data-rona-contract-download-v3]'),unavailable=unavailableNode(card),active=String(entry.context?.contract_status||'').toUpperCase()==='ACTIVE';if(!active){if(existing)existing.remove();return false}if(!entry.document?.storage_object_id){if(existing)existing.remove();if(unavailable){unavailable.textContent='Файл подписанного контракта не опубликован в кабинете';unavailable.setAttribute('aria-disabled','true');unavailable.dataset.ronaContractUnavailable='authoritative-storage-not-materialized'}return false}if(existing&&existing.dataset.storageObjectId===String(entry.document.storage_object_id))return true;if(existing)existing.remove();const b=makeButton(entry,unavailable);if(unavailable){unavailable.replaceWith(b);return true}const anchor=contractAnchor(card),host=anchor?.parentElement||card;host.appendChild(b);return true}
function render(){ensureStyle();syncContractNumberLabels();syncCompanyNameSpelling();syncCompanyControls();let count=0;for(const entry of state.entries.values())if(renderEntry(entry))count++;document.documentElement.dataset.ronaClientContractDownloads=String(count);document.documentElement.dataset.ronaClientContractRuntime='v4';return count>0}
function isContextInteraction(target){const e=target?.closest?.('select,[data-client-id],[data-contract-id],[class*="company"],[class*="contract"],[role="tab"],button,a,[role="button"]');if(!e)return false;if(e.matches('select,[data-client-id],[data-contract-id],[class*="company"],[class*="contract"],[role="tab"]'))return true;const t=low(e.textContent),aria=low(e.getAttribute?.('aria-label')),title=low(e.getAttribute?.('title'));if(t==='мои компании'||t.includes('открыть компанию')||t.includes('переключиться'))return true;for(const entry of state.entries.values()){const c=entry.context||{};for(const k of [c.legal_name,c.current_external_contract_number,c.contract_id,c.client_id].map(low).filter(Boolean))if((t&&t.includes(k))||(aria&&aria.includes(k))||(title&&title.includes(k)))return true}return false}
function scheduleRender(){clearTimeout(state.renderTimer);state.renderTimer=setTimeout(render,180)}
function start(){refresh(true);document.addEventListener('click',e=>{if(isContextInteraction(e.target))scheduleRender()},true);document.addEventListener('change',e=>{if(isContextInteraction(e.target))scheduleRender()},true);window.addEventListener('pageshow',()=>{render();if(Date.now()-state.lastLoad>10000)refresh(true)});setInterval(()=>{if(document.visibilityState==='visible')refresh(true)},REFRESH_MS)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
