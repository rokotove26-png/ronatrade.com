===== home_large outer=5345326 payload_b64=1245836 inner=931663 =====
forms=3
FORM 1: <form id="loginForm">
  <input id="login" name="login" type="text" autocomplete="username" placeholder="Введите логин" />
  <input id="password" name="password" type="password" autocomplete="current-password" placeholder="Введите пароль" />
  <button class="login-btn" type="submit">
  TEXT: Логин Пароль Войти
FORM 2: <form id="requestForm" novalidate>
  <input id="reqName" name="Контактное лицо" type="text" autocomplete="name" placeholder="Ваше имя" required />
  <input id="reqCompany" name="Компания" type="text" autocomplete="organization" placeholder="Название компании" required />
  <input id="reqRegCountry" name="Страна регистрации компании" type="text" autocomplete="country-name" placeholder="Например, Узбекистан" required />
  <select id="reqProduct" name="Нефтепродукт" required>
  <input id="reqDestination" name="Страна назначения груза" type="text" placeholder="Куда требуется поставка" required />
  <input id="reqVolume" name="Ориентировочный объем, тонн" type="number" min="1" step="1" inputmode="decimal" placeholder="Например, 500" />
  <input id="reqPhone" name="Телефон" type="tel" autocomplete="tel" placeholder="+998 ..." />
  <input id="reqEmail" name="E-mail" type="email" autocomplete="email" placeholder="name@company.com" />
  <textarea id="reqComment" name="Комментарий" placeholder="Дополнительные условия, станция назначения, сроки или другая важная информация">
  <input id="reqConsent" type="checkbox" required />
  <button class="request-submit" id="requestSubmit" type="submit">
  TEXT: Параметры заявки Поля со звёздочкой обязательны. Укажите телефон или e-mail — достаточно одного способа связи. Имя * Компания * Страна регистрации * Нефтепродукт * Выберите продукт СПБТ / СУГ Бензин АИ-92 Бензин АИ-95 Дизельное топливо Мазут / топочный мазут Другой нефтепродукт Страна назначения * Ориентировочный объём, т Телефон E-mail * Телефон или e-mail — хотя бы одно из двух полей. Комментарий Я согласен на обработку указанных данных исключительно для рассмотрения коммерческого запроса и обратной связи со стороны RONA Trade. Отправить заявку
FORM 3: <form id="mobileLoginForm">
  <input id="mlogin" type="text" autocomplete="username" placeholder="Введите логин" />
  <input id="mpassword" type="password" autocomplete="current-password" placeholder="Введите пароль" />
  <button class="login-btn" type="submit">
  TEXT: Логин Пароль Войти
NEEDLE 'formsubmit.co' count=1 positions=[928111]
  (); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true})
NEEDLE 'office_kg@ronaoil.com' count=3 positions=[927698, 928130, 928995]
  nRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focu
  il = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } });
  subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> </body> </html>
NEEDLE '/api/forms/trade' count=0 positions=[]
NEEDLE 'fetch(' count=1 positions=[928096]
  ne').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preve
NEEDLE "addEventListener('submit" count=3 positions=[926479, 929830, 929916]
  requestClose'); const requestForm = document.getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestStatus.textContent = ''; requestStatus.className = 'request-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show');
  ponse.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> </body> </html>
  nse.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> </body> </html>
NEEDLE 'addEventListener("submit' count=0 positions=[]
NEEDLE 'preventDefault' count=3 positions=[924686, 926527, 929659]
  </div> </div> </section> </div> <div class="mobile-login-shade" id="mobileLogin" aria-hidden="true"> <aside class="portal-card" aria-label="Личный кабинет клиента"> <button class="modal-close" id="modalClose" type="button" aria-label="Закрыть">×</button> <h2 class="portal-title">Личный кабинет</h2> <div class="portal-sub">Доступ к сделкам, статусам исполнения и разрешённым документам.</div> <form id="mobileLoginForm"> <div class="field"> <label for="mlogin">Логин</label> <input id="mlogin" type="text" autocomplete="username" placeholder="Введите логин" /> </div> <div class="field"> <label for="mpassword">Пароль</label> <input id="mpassword" type="password" autocomplete="current-password" placeholder="Введите пароль" /> </div> <button class="login-btn" type="submit">Войти</button> </form> <p class="portal-note">Доступ предоставляется RONA Trade</p> </aside> </div> <div class="toast" id="toast" role="status" aria-live="polite"></div> <script> const toast = document.getElementById('toast'); let toastTimer; function notify(text){ clearTimeout(toastTimer); toast.textContent = text; toast.classList.add('show'); toastTimer = setTimeout(() => toast.classList.remove('show'), 2600); } document.querySelectorAll('[data-pending]').forEach(el => { el.addEventListener('click', e => { e.preventDefault(); notify('Раздел будет подключён после утверждения главной страницы.'); }); }); const requestBtn = document.getElementById('requestBtn'); const requestModal = document.getElementById('requestModal'); const requestClose = document.getElementById('requestClose'); const requestForm = document.getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestStatus.textContent = ''; requestStatus.className = 'request-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto']
  .getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestStatus.textContent = ''; requestStatus.className = 'request-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transit
  method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> </body> </html>
NEEDLE 'FormData' count=1 positions=[927419]
  equest-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.');
NEEDLE 'JSON.stringify' count=1 positions=[928279]
  if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true');
NEEDLE 'Отправить заявку' count=3 positions=[922569, 928942, 929159]
  ">Ориентировочный объём, т</label> <input id="reqVolume" name="Ориентировочный объем, тонн" type="number" min="1" step="1" inputmode="decimal" placeholder="Например, 500" /> </div> <div class="request-field"> <label for="reqPhone">Телефон</label> <input id="reqPhone" name="Телефон" type="tel" autocomplete="tel" placeholder="+998 ..." /> </div> <div class="request-field"> <label for="reqEmail">E-mail</label> <input id="reqEmail" name="E-mail" type="email" autocomplete="email" placeholder="name@company.com" /> </div> <div class="request-contact-hint">* Телефон или e-mail — хотя бы одно из двух полей.</div> <div class="request-field full"> <label for="reqComment">Комментарий</label> <textarea id="reqComment" name="Комментарий" placeholder="Дополнительные условия, станция назначения, сроки или другая важная информация"></textarea> </div> </div> <label class="request-consent"> <input id="reqConsent" type="checkbox" required /> <span>Я согласен на обработку указанных данных исключительно для рассмотрения коммерческого запроса и обратной связи со стороны RONA Trade.</span> </label> <div class="request-actions"> <button class="request-submit" id="requestSubmit" type="submit">Отправить заявку</button> <div class="request-status" id="requestStatus" role="status" aria-live="polite"></div> </div> </form> <div class="request-success" id="requestSuccess" aria-live="polite"> <div class="request-success-inner"> <div class="request-success-mark">✓</div> <h3>Заявка отправлена</h3> <p>Информация передана в RONA Trade. Мы свяжемся с вами по указанному телефону или e-mail.</p> <button type="button" id="requestSuccessClose">Закрыть</button> </div> </div> </div> </div> </section> </div> <div class="mobile-login-shade" id="mobileLogin" aria-hidden="true"> <aside class="portal-card" aria-label="Личный кабинет клиента"> <button class="modal-close" id="modalClose" type="button" aria-label="Закрыть">×</button> <h2 class="portal-title">Личный кабинет</h2> <div class="portal-sub">Доступ к сделкам, статусам исполнения и разрешённым документам.</div> <form id="mobileLoginForm"> <div class="field"> <label for="mlogin">Логин</label> <input id="mlogin" type="text" autocomplete="username" placeholder="Введите логин" /> </div> <div class="field"> <label for="mpassword">Пароль</label> <input id="mpassword" type="password" autocomplete="current-password" placeholder="Введите пароль" /> </div> <button class="login-btn" type="submit">Войти</button> </form> <p class="portal-note">Доступ предоставляется RONA Trade</p> </aside> </div> <div class="toast" id="toast" role="status" aria-live="polite"></div> <script> const toast = document.getElementById('toast'); let toastTimer; function notify(text){ clearTimeout(toastTimer); toast.textContent = text; toast.classList.add('show'); toastTimer = setTimeout(() => toast.classList.remove('show'), 2600); } document.querySelectorAll('[data-pending]').forEach(el => { el.addEventListener('click', e => { e.preventDefault(); notify('Раздел будет подключён после утверждения главной страницы.'); }); }); const requestBtn = document.getElementById('requestBtn'); const requestModal = document.getElementById('requestModal'); const requestClose = document.getElementById('requestClose'); const requestForm = document.getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid');
  ntries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> </body> </html>
  payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> </body> </html>
===== home_compact outer=10796742 payload_b64=6706212 inner=5026947 =====
forms=3
FORM 1: <form id="loginForm">
  <input autocomplete="username" id="login" name="login" placeholder="Введите логин" type="text"/>
  <input autocomplete="current-password" id="password" name="password" placeholder="Введите пароль" type="password"/>
  <button class="login-btn" type="submit">
  TEXT: Логин Пароль Войти
FORM 2: <form id="requestForm" novalidate="">
  <input autocomplete="name" id="reqName" name="Контактное лицо" placeholder="Ваше имя" required="" type="text"/>
  <input autocomplete="organization" id="reqCompany" name="Компания" placeholder="Название компании" required="" type="text"/>
  <input autocomplete="country-name" id="reqRegCountry" name="Страна регистрации компании" placeholder="Например, Узбекистан" required="" type="text"/>
  <select id="reqProduct" name="Нефтепродукт" required="">
  <input id="reqDestination" name="Страна назначения груза" placeholder="Куда требуется поставка" required="" type="text"/>
  <input id="reqVolume" inputmode="decimal" min="1" name="Ориентировочный объем, тонн" placeholder="Например, 500" step="1" type="number"/>
  <input autocomplete="tel" id="reqPhone" name="Телефон" placeholder="+998 ..." type="tel"/>
  <input autocomplete="email" id="reqEmail" name="E-mail" placeholder="name@company.com" type="email"/>
  <textarea id="reqComment" name="Комментарий" placeholder="Дополнительные условия, станция назначения, сроки или другая важная информация">
  <input id="reqConsent" required="" type="checkbox"/>
  <button class="request-submit" id="requestSubmit" type="submit">
  TEXT: Параметры заявки Поля со звёздочкой обязательны. Укажите телефон или e-mail — достаточно одного способа связи. Имя * Компания * Страна регистрации * Нефтепродукт * Выберите продукт СПБТ / СУГ Бензин АИ-92 Бензин АИ-95 Дизельное топливо Мазут / топочный мазут Другой нефтепродукт Страна назначения * Ориентировочный объём, т Телефон E-mail * Телефон или e-mail — хотя бы одно из двух полей. Комментарий Я согласен на обработку указанных данных исключительно для рассмотрения коммерческого запроса и обратной связи со стороны RONA Trade. Отправить заявку
FORM 3: <form id="mobileLoginForm">
  <input autocomplete="username" id="mlogin" placeholder="Введите логин" type="text"/>
  <input autocomplete="current-password" id="mpassword" placeholder="Введите пароль" type="password"/>
  <button class="login-btn" type="submit">
  TEXT: Логин Пароль Войти
NEEDLE 'formsubmit.co' count=1 positions=[5022136]
  (); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true})
NEEDLE 'office_kg@ronaoil.com' count=3 positions=[5021723, 5022155, 5023020]
  nRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focu
  il = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } });
  subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&portal.parentElement!==nav)nav.appendChild(portal);let btn
NEEDLE '/api/forms/trade' count=0 positions=[]
NEEDLE 'fetch(' count=1 positions=[5022121]
  ne').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preve
NEEDLE "addEventListener('submit" count=3 positions=[5020504, 5023855, 5023941]
  requestClose'); const requestForm = document.getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestStatus.textContent = ''; requestStatus.className = 'request-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show');
  ponse.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&portal.parentElement!==nav)nav.appendChild(portal);let btn=header.querySelector('.rona-mobile-menu-toggle');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='rona-mobile-menu-toggle';btn.textContent='МЕНЮ';btn.setAttribute('aria-expanded','false');btn.setAttribute('aria-label','Открыть меню');header.appendChild(btn)}const close=()=>{document.body.classList.remove('rona-mobile-menu-open');btn.setAttribute('aria-expanded','false')};btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const open=document.body.classList.toggle('rona-mobile-menu-open');btn.setAttribute('aria-expanded',open?'true':'false')});nav.addEventListener('click',e=>{if(e.target.closest('a,button'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});document.addEventListener('click',e=>{if(document.body.classList.contains('rona-mobile-men
  nse.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&portal.parentElement!==nav)nav.appendChild(portal);let btn=header.querySelector('.rona-mobile-menu-toggle');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='rona-mobile-menu-toggle';btn.textContent='МЕНЮ';btn.setAttribute('aria-expanded','false');btn.setAttribute('aria-label','Открыть меню');header.appendChild(btn)}const close=()=>{document.body.classList.remove('rona-mobile-menu-open');btn.setAttribute('aria-expanded','false')};btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const open=document.body.classList.toggle('rona-mobile-menu-open');btn.setAttribute('aria-expanded',open?'true':'false')});nav.addEventListener('click',e=>{if(e.target.closest('a,button'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});document.addEventListener('click',e=>{if(document.body.classList.contains('rona-mobile-menu-open')&&!nav.contains(e.target)&&e.target!==btn)close()});})(); </script> </body> </
NEEDLE 'addEventListener("submit' count=0 positions=[]
NEEDLE 'preventDefault' count=4 positions=[5018711, 5020552, 5023684, 5026460]
  в RONA Trade. Мы свяжемся с вами по указанному телефону или e-mail.</p> <button id="requestSuccessClose" type="button">Закрыть</button> </div> </div> </div> </div> </section> </div> <div aria-hidden="true" class="mobile-login-shade" id="mobileLogin"> <aside aria-label="Личный кабинет клиента" class="portal-card"> <button aria-label="Закрыть" class="modal-close" id="modalClose" type="button">×</button> <h2 class="portal-title">Личный кабинет</h2> <div class="portal-sub">Доступ к сделкам, статусам исполнения и разрешённым документам.</div> <form id="mobileLoginForm"> <div class="field"> <label for="mlogin">Логин</label> <input autocomplete="username" id="mlogin" placeholder="Введите логин" type="text"/> </div> <div class="field"> <label for="mpassword">Пароль</label> <input autocomplete="current-password" id="mpassword" placeholder="Введите пароль" type="password"/> </div> <button class="login-btn" type="submit">Войти</button> </form> <p class="portal-note">Доступ предоставляется RONA Trade</p> </aside> </div> <div aria-live="polite" class="toast" id="toast" role="status"></div> <script> const toast = document.getElementById('toast'); let toastTimer; function notify(text){ clearTimeout(toastTimer); toast.textContent = text; toast.classList.add('show'); toastTimer = setTimeout(() => toast.classList.remove('show'), 2600); } document.querySelectorAll('[data-pending]').forEach(el => { el.addEventListener('click', e => { e.preventDefault(); notify('Раздел будет подключён после утверждения главной страницы.'); }); }); const requestBtn = document.getElementById('requestBtn'); const requestModal = document.getElementById('requestModal'); const requestClose = document.getElementById('requestClose'); const requestForm = document.getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestStatus.textContent = ''; requestStatus.className = 'request-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto']
  .getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestStatus.textContent = ''; requestStatus.className = 'request-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transit
  method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&portal.parentElement!==nav)nav.appendChild(portal);let btn=header.querySelector('.rona-mobile-menu-toggle');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='rona-mobile-menu-toggle';btn.textContent='МЕНЮ';btn.setAttribute('aria-expanded','false');btn.setAttribute('aria-label','Открыть меню');header.appendChild(btn)}const close=()=>{document.body.classList.remove('rona-mobile-menu-open');btn.setAttribute('aria-expanded','false')};btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const open=document.body.classList.toggle('rona-mobile-menu-open');btn.setAttribute('aria-expanded',open?'true':'false')});nav.addEventListener('click',e=>{if(e.target.closest('a,button')
  orm:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&portal.parentElement!==nav)nav.appendChild(portal);let btn=header.querySelector('.rona-mobile-menu-toggle');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='rona-mobile-menu-toggle';btn.textContent='МЕНЮ';btn.setAttribute('aria-expanded','false');btn.setAttribute('aria-label','Открыть меню');header.appendChild(btn)}const close=()=>{document.body.classList.remove('rona-mobile-menu-open');btn.setAttribute('aria-expanded','false')};btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const open=document.body.classList.toggle('rona-mobile-menu-open');btn.setAttribute('aria-expanded',open?'true':'false')});nav.addEventListener('click',e=>{if(e.target.closest('a,button'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});document.addEventListener('click',e=>{if(document.body.classList.contains('rona-mobile-menu-open')&&!nav.contains(e.target)&&e.target!==btn)close()});})(); </script> </body> </html>
NEEDLE 'FormData' count=1 positions=[5021444]
  equest-status'; setTimeout(() => document.getElementById('reqName').focus(), 180); } function closeRequestModal(){ requestModal.classList.remove('open'); requestModal.setAttribute('aria-hidden','true'); } requestBtn.addEventListener('click', openRequestModal); requestClose.addEventListener('click', closeRequestModal); requestSuccessClose.addEventListener('click', closeRequestModal); requestModal.addEventListener('click', e => { if(e.target === requestModal) closeRequestModal(); }); requestForm.addEventListener('submit', async e => { e.preventDefault(); const phone = document.getElementById('reqPhone').value.trim(); const email = document.getElementById('reqEmail').value.trim(); requestStatus.className = 'request-status'; requestStatus.textContent = ''; if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.');
NEEDLE 'JSON.stringify' count=1 positions=[5022304]
  if(!requestForm.checkValidity()){ requestForm.reportValidity(); requestStatus.textContent = 'Проверьте обязательные поля.'; requestStatus.classList.add('error'); return; } if(!phone && !email){ requestStatus.textContent = 'Укажите телефон или e-mail для связи.'; requestStatus.classList.add('error'); document.getElementById('reqPhone').focus(); return; } requestSubmit.disabled = true; requestSubmit.textContent = 'Отправляем…'; requestStatus.textContent = 'Передаём заявку в RONA Trade…'; const payload = Object.fromEntries(new FormData(requestForm).entries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true');
NEEDLE 'Отправить заявку' count=3 positions=[5016887, 5022967, 5023184]
  option>Другой нефтепродукт</option> </select> </div> <div class="request-field"> <label for="reqDestination">Страна назначения *</label> <input id="reqDestination" name="Страна назначения груза" placeholder="Куда требуется поставка" required="" type="text"/> </div> <div class="request-field"> <label for="reqVolume">Ориентировочный объём, т</label> <input id="reqVolume" inputmode="decimal" min="1" name="Ориентировочный объем, тонн" placeholder="Например, 500" step="1" type="number"/> </div> <div class="request-field"> <label for="reqPhone">Телефон</label> <input autocomplete="tel" id="reqPhone" name="Телефон" placeholder="+998 ..." type="tel"/> </div> <div class="request-field"> <label for="reqEmail">E-mail</label> <input autocomplete="email" id="reqEmail" name="E-mail" placeholder="name@company.com" type="email"/> </div> <div class="request-contact-hint">* Телефон или e-mail — хотя бы одно из двух полей.</div> <div class="request-field full"> <label for="reqComment">Комментарий</label> <textarea id="reqComment" name="Комментарий" placeholder="Дополнительные условия, станция назначения, сроки или другая важная информация"></textarea> </div> </div> <label class="request-consent"> <input id="reqConsent" required="" type="checkbox"/> <span>Я согласен на обработку указанных данных исключительно для рассмотрения коммерческого запроса и обратной связи со стороны RONA Trade.</span> </label> <div class="request-actions"> <button class="request-submit" id="requestSubmit" type="submit">Отправить заявку</button> <div aria-live="polite" class="request-status" id="requestStatus" role="status"></div> </div> </form> <div aria-live="polite" class="request-success" id="requestSuccess"> <div class="request-success-inner"> <div class="request-success-mark">✓</div> <h3>Заявка отправлена</h3> <p>Информация передана в RONA Trade. Мы свяжемся с вами по указанному телефону или e-mail.</p> <button id="requestSuccessClose" type="button">Закрыть</button> </div> </div> </div> </div> </section> </div> <div aria-hidden="true" class="mobile-login-shade" id="mobileLogin"> <aside aria-label="Личный кабинет клиента" class="portal-card"> <button aria-label="Закрыть" class="modal-close" id="modalClose" type="button">×</button> <h2 class="portal-title">Личный кабинет</h2> <div class="portal-sub">Доступ к сделкам, статусам исполнения и разрешённым документам.</div> <form id="mobileLoginForm"> <div class="field"> <label for="mlogin">Логин</label> <input autocomplete="username" id="mlogin" placeholder="Введите логин" type="text"/> </div> <div class="field"> <label for="mpassword">Пароль</label> <input autocomplete="current-password" id="mpassword" placeholder="Введите пароль" type="password"/> </div> <button class="login-btn" type="submit">Войти</button> </form> <p class="portal-note">Доступ предоставляется RONA Trade</p> </aside> </div> <div aria-live="polite" class="toast" id="toast" role="status"></div> <script> const toast = document.getElementById('toast'); let toastTimer; function notify(text){ clearTimeout(toastTimer); toast.textContent = text; toast.classList.add('show'); toastTimer = setTimeout(() => toast.classList.remove('show'), 2600); } document.querySelectorAll('[data-pending]').forEach(el => { el.addEventListener('click', e => { e.preventDefault(); notify('Раздел будет подключён после утверждения главной страницы.'); }); }); const requestBtn = document.getElementById('requestBtn'); const requestModal = document.getElementById('requestModal'); const requestClose = document.getElementById('requestClose'); const requestForm = document.getElementById('requestForm'); const requestSubmit = document.getElementById('requestSubmit'); const requestStatus = document.getElementById('requestStatus'); const requestSuccess = document.getElementById('requestSuccess'); const requestSuccessClose = document.getElementById('requestSuccessClose'); const requestFormTitle = requestForm.querySelector('.request-form-title'); const requestFormSub = requestForm.querySelector('.request-form-sub'); const requestGrid = requestForm.querySelector('.request-grid'); const requestConsent = requestForm.querySelector('.request-consent'); const requestActions = requestForm.querySelector('.request-actions'); function openRequestModal(){ requestModal.classList.add('open'); requestModal.setAttribute('aria-hidden','false'); requestSt
  ntries()); delete payload['']; payload['_subject'] = `Новая заявка с сайта RONA Trade — ${payload['Компания'] || 'новый клиент'}`; payload['_template'] = 'table'; payload['_captcha'] = 'false'; payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&porta
  payload['_replyto'] = email || 'office_kg@ronaoil.com'; payload['Источник'] = 'Главная страница RONA Trade / кнопка «Оставить заявку»'; payload['Дата отправки'] = new Date().toLocaleString('ru-RU'); payload['Language'] = 'RU'; try{payload['URL страницы / источник'] = window.top.location.href;}catch(_){payload['URL страницы / источник'] = window.location.href;} try{ const response = await fetch('https://formsubmit.co/ajax/office_kg@ronaoil.com', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body:JSON.stringify(payload) }); if(!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json().catch(() => ({})); if(data.success === false) throw new Error(data.message || 'Submission rejected'); requestForm.reset(); requestFormTitle.style.display = 'none'; requestFormSub.style.display = 'none'; requestGrid.style.display = 'none'; requestConsent.style.display = 'none'; requestActions.style.display = 'none'; requestSuccess.classList.add('show'); }catch(err){ console.error(err); requestStatus.textContent = 'Не удалось отправить заявку. Попробуйте ещё раз или напишите на office_kg@ronaoil.com.'; requestStatus.classList.add('error'); }finally{ requestSubmit.disabled = false; requestSubmit.textContent = 'Отправить заявку'; } }); function resetRequestView(){ requestFormTitle.style.display = ''; requestFormSub.style.display = ''; requestGrid.style.display = ''; requestConsent.style.display = ''; requestActions.style.display = ''; requestSuccess.classList.remove('show'); } requestModal.addEventListener('transitionend', () => { if(!requestModal.classList.contains('open')) resetRequestView(); }); function loginStub(e){ e.preventDefault(); notify('Интерфейс входа готов. Серверная авторизация будет подключена после утверждения дизайна.'); } document.getElementById('loginForm').addEventListener('submit', loginStub); document.getElementById('mobileLoginForm').addEventListener('submit', loginStub); const portalTop = document.getElementById('portalTop'); const consultingTop = document.getElementById('consultingTop'); const portalCard = document.getElementById('portalCard'); const mobileLogin = document.getElementById('mobileLogin'); const modalClose = document.getElementById('modalClose'); consultingTop.addEventListener('click', () => { notify('Раздел RONA Investments — «Консалтинг» будет подключён отдельной страницей.'); }); portalTop.addEventListener('click', () => { if (window.matchMedia('(max-width:900px)').matches) { mobileLogin.classList.add('open'); mobileLogin.setAttribute('aria-hidden','false'); setTimeout(() => document.getElementById('mlogin').focus(), 160); } else { portalCard.animate([ {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'}, {transform:'translateY(-5px)', boxShadow:'0 36px 92px rgba(225,6,0,.20)'}, {transform:'translateY(0)', boxShadow:'0 30px 80px rgba(0,0,0,.28)'} ], {duration:480,easing:'ease-out'}); document.getElementById('login').focus({preventScroll:true}); } }); modalClose.addEventListener('click', () => { mobileLogin.classList.remove('open'); mobileLogin.setAttribute('aria-hidden','true'); }); mobileLogin.addEventListener('click', e => { if(e.target === mobileLogin) modalClose.click(); }); document.addEventListener('keydown', e => { if(e.key !== 'Escape') return; if(requestModal.classList.contains('open')) closeRequestModal(); else if(mobileLogin.classList.contains('open')) modalClose.click(); }); </script> <script id="rona-home-mobile-menu-runtime-v1"> (()=>{'use strict';if(!matchMedia('(max-width:767px)').matches)return;const header=document.querySelector('header'),nav=header&&header.querySelector('.nav');if(!header||!nav)return;const portal=document.getElementById('portalTop');if(portal&&portal.parentElement!==nav)nav.appendChild(portal);let btn=header.querySelector('.rona-mobile-menu-toggle');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='rona-mobile-menu-toggle';btn.textCo