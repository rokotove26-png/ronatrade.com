import http from 'node:http';
import {readFile,stat} from 'node:fs/promises';
import {extname,join,normalize} from 'node:path';
import {chromium} from 'playwright';
const ROOT=process.cwd(),SOURCE_ROOT=process.env.RONA_OWNER_SOURCE_ROOT||ROOT,BASE_ROOT=process.env.RONA_OWNER_BASE_ROOT||'',LABEL=process.env.RONA_OWNER_RETEST_LABEL||'TARGET',DIST=join(ROOT,'dist'),BASE_DIST=BASE_ROOT?join(BASE_ROOT,'dist'):null;
const norm=v=>String(v??'').replace(/\s+/g,' ').trim(),upper=v=>norm(v).toUpperCase(),sleep=ms=>new Promise(r=>setTimeout(r,ms));
const NAV='#nav [data-page],[data-rona-client-nav] [data-page],nav [data-page]';
const TDEALS=new Set(['CLOSED','COMPLETED','DONE','CANCELLED','RESOURCE_DENIED']),TAPPS=new Set(['DEAL_REGISTERED','ARCHIVED','CANCELLED','REJECTED','CLOSED']);
const FIX={A:{client_id:'RONA-C004',contract_id:'RONA-C004-CTR-2026-001',bootstrap:'ООО ТК НИК-ОЙЛ',legal:'Общество с ограниченной ответственностью Топливная компания «НИК-ОЙЛ»',external:'RONA-C004-CTR-2026-001'},B:{client_id:'CLIENT-B',contract_id:'CONTRACT-B',bootstrap:'BOOTSTRAP ALIAS B',legal:'BETA ENERGY LLC',external:'EXT-B'}};
const DEALS={A:[['DEAL-2026-007','EXECUTING',null],['DEAL-2026-008','EXECUTING',null]],B:[['DEAL-2099-201','EXECUTING',null],['DEAL-2099-202','CANCELLED','2099-01-02T00:00:00Z']]};
const APPS={A:[['RONA-C004-IN-2026-001','CANCELLED',null,470,464,'USD'],['RONA-C004-IN-2026-002','CANCELLED',null,470,464,'USD'],['RONA-C004-IN-2026-003','DEAL_REGISTERED','DEAL-2026-008',470,464,'USD'],['RONA-C004-IN-2026-004','DEAL_REGISTERED','DEAL-2026-007',250,454,'USD']],B:[['APP-B-1','UNDER_REVIEW',null,10,100,'USD'],['APP-B-2','DEAL_REGISTERED','DEAL-2099-201',20,200,'USD'],['APP-B-3','CANCELLED',null,30,300,'USD']]};
const DOCS={A:5,B:3};
if(SOURCE_ROOT!==ROOT)throw new Error(`OWNER_RETEST_ROOT_OWNERSHIP_MISMATCH served=${ROOT} source=${SOURCE_ROOT}`);
const API_SOURCE=await readFile(join(SOURCE_ROOT,'supabase/functions/rona-portal-api/index.ts'),'utf8');
const APPLICATION_AMOUNT_PROJECTION=["portal_private.owner_application_workflow","a.status='DEAL_REGISTERED'","w.business_status='DEAL'","coalesce(w.counter_offer_used,false)=false","w.finalized_at is not null","passport_amount","passport_currency","passport_amount_source","passport_application_id","FINALIZED_APPLICATION_COMMERCIAL_TERMS"].every(token=>API_SOURCE.includes(token));
const requests=[];
function commercialSource(name,id){const row=APPS[name].find(x=>x[2]===id&&upper(x[1])==='DEAL_REGISTERED');if(!row)return null;const[application_id,status,deal_id,quantity_tonnes,proposed_price,currency]=row;return{deal_id,application_id,status,quantity_tonnes,proposed_price,currency,obligation_amount:Number(quantity_tonnes)*Number(proposed_price),business_status:'DEAL',counter_offer_used:false,finalized_at:'2026-09-04T16:06:43.236673Z',source:'FINALIZED_APPLICATION_COMMERCIAL_TERMS'}}
function payFromCanonicalApplication(name,id){const src=commercialSource(name,id),enabled=APPLICATION_AMOUNT_PROJECTION&&src;return{payment_status:'NOT_DUE',payment_label:'Оплата не наступила',payment_received_amount:null,payment_obligation_amount:null,payment_currency:null,payment_percent:null,payment_source:'DEAL_FINANCE_STATUS',passport_amount:enabled?src.obligation_amount:null,passport_currency:enabled?src.currency:null,passport_amount_source:enabled?src.source:null,passport_application_id:enabled?src.application_id:null}}
function raw(name){const f=FIX[name],deals=DEALS[name].map(([deal_id,business_status,closed_at])=>({deal_id,business_status,current_status:business_status,current_status_label:business_status==='EXECUTING'?'В исполнении':business_status==='RESOURCE_CONFIRMED'?'Ресурс подтверждён':'Завершена',status_source:'OPERATIONS_DEAL_BUSINESS_STATUS',...payFromCanonicalApplication(name,deal_id),resource_status:'RESOURCE_CONFIRMED',resource_label:'Ресурс подтвержден',resource_source:'OPERATIONS_DEAL_BUSINESS_STATUS',opened_at:'2099-01-01T00:00:00Z',closed_at,updated_at:'2099-01-02T00:00:00Z'}));const applications=APPS[name].map(([application_id,status,deal_id,quantity_tonnes,proposed_price,proposed_currency],i)=>({application_id,product:name==='A'?'СУГ / СПБТ':`PRODUCT-${name}-${i+1}`,quantity_tonnes,delivery_period_from:'2099-01-01',delivery_period_to:'2099-01-31',delivery_basis:'CPT',destination:`DEST-${name}-${i+1}`,payment_terms:'PREPAYMENT',price_mode:'FIXED',proposed_price,proposed_currency,status,deal_id,submitted_at:'2099-01-01T00:00:00Z',updated_at:'2099-01-02T00:00:00Z'}));const documents=Array.from({length:DOCS[name]},(_,i)=>({document_id:`DOC-${name}-${i+1}`,document_type:'DOCUMENT',authoritative_filename:`Document-${name}-${i+1}.pdf`,deal_id:deals[Math.min(i,deals.length-1)]?.deal_id||null,updated_at:'2099-01-02T00:00:00Z',storage_object_id:null}));return{projection_contract:'ADMIN_CLIENT_SERVER_V1',contract:{client_id:f.client_id,legal_name:f.legal,registration_country:'TEST',registered_address:null,contact_phone:null,contract_id:f.contract_id,current_external_contract_number:f.external,contract_status:'ACTIVE',effective_from:'2099-01-01',effective_to:'2099-12-31',reference_status:'CONFIRMED'},applications,deals,documents,payments:[],prices:[]}}
const RAW={A:raw('A'),B:raw('B')};
const activeDeal=d=>!d.closed_at&&!TDEALS.has(upper(d.current_status||d.business_status||d.status));
function expected(name){const r=RAW[name],first=r.deals.find(activeDeal),src=commercialSource(name,first.deal_id);return{client:r.contract.client_id,contract:r.contract.contract_id,legal:r.contract.legal_name,external:r.contract.current_external_contract_number,applications:r.applications.filter(a=>!norm(a.deal_id)&&!TAPPS.has(upper(a.status))).length,deals:r.deals.filter(activeDeal).length,documents:r.documents.length,array_lengths:{applications:r.applications.length,deals:r.deals.length,documents:r.documents.length},active_ids:r.deals.filter(activeDeal).map(x=>x.deal_id),terminal_ids:r.deals.filter(d=>!activeDeal(d)).map(x=>x.deal_id),passport:first.deal_id,amount:src.obligation_amount,currency:src.currency,source_application_id:src.application_id}}
const EXP={A:expected('A'),B:expected('B')};
const SOURCE_MAP={endpoint:'/portal/api/v1/client/context',passport:{relation:'portal_private.client_applications JOIN portal_private.owner_application_workflow',amount:'quantity_tonnes * proposed_price',currency:'proposed_currency',filter:"status=DEAL_REGISTERED AND business_status=DEAL AND counter_offer_used=false AND finalized_at IS NOT NULL",backend:'projectClientDeals -> dedicated Passport commercial projection',api_amount:'data.deals[].passport_amount',api_currency:'data.deals[].passport_currency',api_source:'data.deals[].passport_amount_source',api_application:'data.deals[].passport_application_id',consumer:'client-deals-authoritative-v1 authoritativeAmount',dom:'native Passport СУММА'},kpi:{legal_name:'data.contract.legal_name',applications:'client-applications-live-render-v1 isActive: !terminal status && !deal_id',deals:'client-deals-authoritative-v1 activeDeals: canonical DEAL id && !closed_at && !terminal business/current status',documents:'all authoritative current-context data.documents[]',reference:'canonical product predicates'}};
function context(name){const f=FIX[name];return{client_id:f.client_id,legal_name:f.bootstrap,registration_country:'TEST',contract_id:f.contract_id,current_external_contract_number:f.external,contract_status:'ACTIVE',effective_from:'2099-01-01',effective_to:'2099-12-31'}}
function stages(){return[['contract','DONE'],['documents','DONE'],['resource','CURRENT'],['payment','PENDING'],['logistics','PENDING'],['close','PENDING']].map(([key,state])=>({key,state,detail:`${key} fixture status`}))}
function nameFor(c,k){return Object.keys(FIX).find(n=>FIX[n].client_id===c&&FIX[n].contract_id===k)||'A'}
function send(res,body){const t=JSON.stringify(body);res.writeHead(200,{'content-type':'application/json; charset=utf-8','cache-control':'no-store','connection':'close','content-length':Buffer.byteLength(t)});res.end(t)}
async function api(label,req,res,u){const c=u.searchParams.get('clientId')||'',k=u.searchParams.get('contractId')||'',name=nameFor(c,k),f=FIX[name],r=RAW[name];let b;if(u.pathname==='/portal/api/v1/client/bootstrap')b={ok:true,data:{contexts:[context('A'),context('B')],selected_context:context('A'),requires_context_selection:false}};else if(u.pathname==='/portal/api/v1/client/context')b={ok:true,data:r};else if(u.pathname==='/portal/api/v1/client/applications-projection')b={ok:true,data:{client_id:f.client_id,contract_id:f.contract_id,applications:r.applications}};else if(u.pathname==='/portal/api/v1/client/prices')b={ok:true,client_id:f.client_id,contract_id:f.contract_id,prices:[{publication_item_id:`PRICE-${name}`,publication_id:`PUB-${name}`,client_id:f.client_id,contract_id:f.contract_id,product:`PRODUCT-${name}`,basis:'CPT',price:100,currency:'USD',producer:`PRODUCER-${name}`,supplier:`SUPPLIER-${name}`}]};else if(u.pathname==='/portal/api/v1/client/deal-documents/state')b={ok:true,client_id:f.client_id,contract_id:f.contract_id,deals:r.deals.map(d=>({deal_id:d.deal_id,client_id:f.client_id,contract_id:f.contract_id,realization_status:{source:'SERVER_AUTHORITATIVE_REALIZATION_V1',current_stage_key:'resource',stages:stages()}}))};else if(u.pathname.startsWith('/portal/api/v1/client/'))b={ok:true,data:{}};else return false;const p=b.data||b;requests.push({label,path:u.pathname,client_id:c,contract_id:k,source:norm(req.headers['x-rona-client-source']||'UNATTRIBUTED'),response_client_id:norm(p.contract?.client_id||p.client_id),response_contract_id:norm(p.contract?.contract_id||p.contract_id)});if(u.pathname==='/portal/api/v1/client/context')await sleep(name==='B'?180:35);send(res,b);return true}
function mime(p){const e=extname(p).toLowerCase();return e==='.html'?'text/html; charset=utf-8':e==='.js'?'application/javascript; charset=utf-8':e==='.css'?'text/css; charset=utf-8':e==='.svg'?'image/svg+xml':e==='.png'?'image/png':'application/octet-stream'}
async function start(dist,label){const html=join(dist,'portal/client.html'),server=http.createServer(async(req,res)=>{try{const u=new URL(req.url||'/','http://127.0.0.1');if(u.pathname==='/portal/client'||u.pathname==='/portal/client/'){const b=await readFile(html);res.writeHead(200,{'content-type':'text/html; charset=utf-8','cache-control':'no-store'});return res.end(b)}if(await api(label,req,res,u))return;const clean=normalize(u.pathname).replace(/^([.][.][/\\])+/, '').replace(/^[/\\]+/,'');const p=join(dist,clean);if(p.startsWith(dist)){try{if((await stat(p)).isFile()){const b=await readFile(p);res.writeHead(200,{'content-type':mime(p),'cache-control':'no-store'});return res.end(b)}}catch{}}res.writeHead(404);res.end('not found')}catch(e){res.writeHead(500);res.end(String(e))}});await new Promise(r=>server.listen(0,'127.0.0.1',r));return{server,origin:`http://127.0.0.1:${server.address().port}`}}
const stop=s=>new Promise(r=>{s.closeAllConnections?.();s.close(()=>r())}),fail=(out,code,detail={})=>{out.push({code,detail});console.log('ASSERTION_FAILED',code,JSON.stringify(detail))};
async function wait(page,fn,arg=null,timeout=3000){try{await page.waitForFunction(fn,arg,{timeout});return true}catch{return false}}
async function ensureProjection(page,e){const apiReady=await wait(page,()=>!!window.RONA_CLIENT_CONTEXT?.whenCurrentProjection&&!!window.RONA_CLIENT_CONTEXT?.getCurrentContext,null,5000);if(!apiReady)return{ok:false,reason:'CONTEXT_API_UNAVAILABLE'};return page.evaluate(async e=>{const out={ok:false,client_id:e.client_id,contract_id:e.contract_id};try{const current=window.RONA_CLIENT_CONTEXT.getCurrentContext?.();out.selected=current?{client_id:String(current.client_id||''),contract_id:String(current.contract_id||'')}:null;if(!current||current.client_id!==e.client_id||current.contract_id!==e.contract_id){out.reason='SELECTED_CONTEXT_MISMATCH';return out}const data=await window.RONA_CLIENT_CONTEXT.whenCurrentProjection('owner-retest-adapter-readiness');const projection=window.RONA_CLIENT_CONTEXT.getCurrentProjection?.()||data||null;out.projection=projection?{projection_contract:String(projection.projection_contract||''),client_id:String(projection.contract?.client_id||projection.client_id||''),contract_id:String(projection.contract?.contract_id||projection.contract_id||''),deals:Array.isArray(projection.deals)?projection.deals.length:null,applications:Array.isArray(projection.applications)?projection.applications.length:null}:null;out.ok=!!projection&&out.projection.client_id===e.client_id&&out.projection.contract_id===e.contract_id;if(!out.ok)out.reason='PROJECTION_SCOPE_MISMATCH';return out}catch(error){out.reason='WHEN_CURRENT_PROJECTION_ERROR';out.error=String(error?.stack||error);const current=window.RONA_CLIENT_CONTEXT?.getCurrentContext?.();out.current=current?{client_id:String(current.client_id||''),contract_id:String(current.contract_id||'')}:null;return out}},e)}
async function nav(page,name){await page.evaluate(({name,sel})=>{const b=[...document.querySelectorAll(sel)].find(x=>x.dataset.page===name);if(!b)throw new Error('NAV_MISSING:'+name);b.click()},{name,sel:NAV});await sleep(70)}
async function typeSample(page,name){await nav(page,name);await sleep(160);return page.evaluate(({name,sel})=>{const nrm=v=>String(v??'').replace(/\s+/g,' ').trim(),vis=n=>n&&getComputedStyle(n).display!=='none'&&n.getBoundingClientRect().width>0;const root=document.querySelector(`#page-${name},[data-page-panel="${name}"],[data-page-id="${name}"]`),one=s=>[...document.querySelectorAll(s)].find(vis),font=n=>n?parseFloat(getComputedStyle(n).fontSize):null,clip=n=>!!n&&((['hidden','clip'].includes(getComputedStyle(n).overflowX)&&n.scrollWidth>n.clientWidth+1)||(['hidden','clip'].includes(getComputedStyle(n).overflowY)&&n.scrollHeight>n.clientHeight+1));const n=one(sel),t=root?.id?one(`#${root.id} .page-head h1`):null,s=root?.id?one(`#${root.id} .page-head .sub`):null,l=root?[...root.querySelectorAll('*')].find(x=>vis(x)&&x.childElementCount===0&&nrm(x.textContent).length>=3):null;return{body:parseFloat(getComputedStyle(document.body).fontSize),nav:font(n),title:font(t),sub:font(s),leaf:font(l),clip:[n,t,s,l].some(clip)}},{name,sel:NAV})}
async function companyProof(page,e){return page.evaluate(e=>{const nrm=v=>String(v??'').replace(/\s+/g,' ').trim(),vis=n=>n&&getComputedStyle(n).display!=='none'&&n.getBoundingClientRect().width>0;const card=[...document.querySelectorAll('[data-rona-client-id][data-rona-client-contract-id]')].find(x=>vis(x)&&x.dataset.ronaClientId===e.client&&x.dataset.ronaClientContractId===e.contract)||[...document.querySelectorAll('.company-card,.client-card,.contract-card,[data-company-card],main .panel')].find(x=>vis(x)&&nrm(x.innerText).includes(e.client)&&nrm(x.innerText).includes(e.contract));const text=nrm(card?.innerText);const metric=needle=>{for(const n of card?.querySelectorAll('*')||[]){if(n.childElementCount||!nrm(n.textContent).toLowerCase().includes(needle))continue;for(let p=n.parentElement,i=0;p&&p!==card&&i<4;p=p.parentElement,i++){const m=nrm(p.innerText).match(/\b(\d+)\b/);if(m)return Number(m[1])}}return null};return{found:!!card,text,contains:{client:text.includes(e.client),contract:text.includes(e.contract),legal:text.includes(e.legal),external:text.includes(e.external)},metrics:{applications:metric('заяв'),deals:metric('сдел'),documents:metric('документ')}}},e)}
const OWNER_TYPO_MARK='RONA_CLIENT_OWNER_TYPOGRAPHY_110_V3_COMPUTED';
async function ownerTypoScope(page,selector){return page.evaluate(({selector,mark})=>{const nrm=v=>String(v??'').replace(/\s+/g,' ').trim(),vis=n=>{if(!n||!n.isConnected||n.hidden)return false;const s=getComputedStyle(n),r=n.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&Number(s.opacity)!==0&&r.width>0&&r.height>0},direct=n=>{if(n.matches?.('input,select,textarea,button,option'))return true;for(const x of n.childNodes||[])if(x.nodeType===Node.TEXT_NODE){const t=nrm(x.textContent);if(t&&!(t.length===1&&!/[A-Za-zА-Яа-яЁё0-9]/u.test(t)))return true}return false},excluded=n=>!!n.closest?.('svg,canvas,picture,img,video,#page-analytics,#analyticsPage,[data-page-panel="analytics"],[data-page-id="analytics"]')||n.getAttribute?.('aria-hidden')==='true'||/(?:icon|logo|glyph)/i.test(String(n.className||'')),clip=n=>{const s=getComputedStyle(n);return((['hidden','clip'].includes(s.overflowX)&&n.scrollWidth>n.clientWidth+1)||(['hidden','clip'].includes(s.overflowY)&&n.scrollHeight>n.clientHeight+1))};const roots=[...document.querySelectorAll(selector)].filter(vis),nodes=[...new Set(roots.flatMap(root=>[root,...root.querySelectorAll('*')]))].filter(n=>vis(n)&&direct(n)&&!excluded(n)),rows=nodes.map(n=>{const base=Number(n.getAttribute('data-rona-owner-typo-base')),font=parseFloat(getComputedStyle(n).fontSize),ratio=Number.isFinite(base)&&base>0?font/base:null;return{text:nrm(n.textContent).slice(0,100),tag:n.tagName,className:String(n.className||'').slice(0,100),base:Number.isFinite(base)?base:null,font,ratio,marked:n.getAttribute('data-rona-owner-typo-scaled')==='1.10',clip:clip(n)}});return{html_marker:document.documentElement.dataset.ronaClientOwnerTypography||'',eligible:rows.length,marked:rows.filter(x=>x.marked).length,unmarked:rows.filter(x=>!x.marked).slice(0,15),bad:rows.filter(x=>x.marked&&(x.ratio<1.075||x.ratio>1.125)).slice(0,15),clipped:rows.filter(x=>x.clip).slice(0,15),rows:rows.slice(0,40),expected_marker:mark}}, {selector,mark})}
async function ownerTypoSection(page,name){await nav(page,name);await sleep(220);const selector=`#page-${name},#${name}Page,[data-page-panel="${name}"],[data-page-id="${name}"]`;return ownerTypoScope(page,selector)}
function ownerTypoScopePass(scope,min=3){return scope&&scope.html_marker===OWNER_TYPO_MARK&&scope.eligible>=min&&scope.marked===scope.eligible&&scope.bad.length===0&&scope.clipped.length===0}

console.log('OWNER_RETEST_ROOT_OWNERSHIP',JSON.stringify({label:LABEL,served_root:ROOT,served_dist:DIST,source_root:SOURCE_ROOT,source_api:join(SOURCE_ROOT,'supabase/functions/rona-portal-api/index.ts'),base_root:BASE_ROOT||null,client_id:FIX.A.client_id,contract_id:FIX.A.contract_id}));console.log('PRODUCTION_SOURCE_MAP',JSON.stringify(SOURCE_MAP));console.log('API_SOURCE_APPLICATION_AMOUNT_PROJECTION',JSON.stringify({enabled:APPLICATION_AMOUNT_PROJECTION,required_tokens:SOURCE_MAP.passport}));console.log('AUTHORITATIVE_C004_UPSTREAM_SNAPSHOT',JSON.stringify(APPS.A.filter(x=>x[2]).map(x=>commercialSource('A',x[2]))));console.log('MY_COMPANIES_MIXED_STATUS_FIXTURE',JSON.stringify({array_lengths:EXP.A.array_lengths,semantic:{applications:EXP.A.applications,deals:EXP.A.deals,documents:EXP.A.documents},applications:RAW.A.applications.map(x=>[x.status,x.deal_id]),deals:RAW.A.deals.map(x=>[x.current_status,x.closed_at])}));
const target=await start(DIST,LABEL),base=BASE_DIST?await start(BASE_DIST,'BASE'):null,failures=[];let browser;
try{browser=await chromium.launch({headless:true});const ctx=await browser.newContext({viewport:{width:1440,height:1000}}),page=await ctx.newPage();page.on('console',msg=>{if(['error','warning'].includes(msg.type()))console.log('BROWSER_CONSOLE',JSON.stringify({label:LABEL,type:msg.type(),text:msg.text()}))});page.on('pageerror',error=>console.log('BROWSER_PAGE_ERROR',JSON.stringify({label:LABEL,error:String(error?.stack||error)})));page.on('requestfailed',req=>console.log('BROWSER_REQUEST_FAILED',JSON.stringify({label:LABEL,url:req.url(),failure:req.failure()})));await page.goto(target.origin+'/portal/client',{waitUntil:'domcontentloaded',timeout:30000});const readiness=await ensureProjection(page,FIX.A);console.log('RONA_OWNER_CONTEXT_FIRST_PROJECTION',JSON.stringify({label:LABEL,...readiness}));if(!readiness.ok)throw new Error(`OWNER_CONTEXT_ADAPTER_READINESS:${JSON.stringify(readiness)}`);const navs=await page.evaluate(sel=>[...document.querySelectorAll(sel)].filter(x=>x.offsetWidth||x.offsetHeight).map(x=>({page:x.dataset.page,text:String(x.textContent||'').replace(/\s+/g,' ').trim()})),NAV);console.log('PRE_DOM_ADAPTER_PROOF',JSON.stringify({nav_selector:NAV,obsolete_shell_header_selector:false,navs}));for(const sec of ['companies','deals','prices','applications']){await nav(page,sec);const before=requests.length,t=Date.now();await nav(page,'home');const ready=await wait(page,arg=>document.documentElement.dataset.ronaClientHomeState==='ready'&&window.RONA_CLIENT_CONTEXT?.getCurrentProjection?.()?.contract?.client_id===arg,FIX.A.client_id,2500),ms=Date.now()-t,req=requests.slice(before).filter(x=>x.label===LABEL&&x.path==='/portal/api/v1/client/context');const d={section:sec,ready,ms,context_requests:req};if(!ready||ms>300)fail(failures,'HOME_REENTRY_READY',d);if(req.length)fail(failures,'HOME_REENTRY_CONTEXT_OWNER',d)}
await nav(page,'companies');await sleep(450);const cp=await companyProof(page,EXP.A),cproof={array_lengths:EXP.A.array_lengths,expected:{applications:EXP.A.applications,deals:EXP.A.deals,documents:EXP.A.documents},visible:cp.metrics,legal:cp.contains.legal};console.log('MY_COMPANIES_SEMANTIC_SOURCE_PROOF',JSON.stringify(cproof));if(!cp.found||!Object.values(cp.contains).every(Boolean)||cp.metrics.applications!==EXP.A.applications||cp.metrics.deals!==EXP.A.deals||cp.metrics.documents!==EXP.A.documents)fail(failures,'CURRENT_COMPANY_CARD_AND_COUNTERS',{cp,cproof});
await nav(page,'deals');await sleep(500);const projection=await page.evaluate(id=>{const d=window.RONA_CLIENT_CONTEXT.getCurrentProjection().deals.find(x=>x.deal_id===id);return{amount:d?.passport_amount,currency:d?.passport_currency,source:d?.passport_amount_source,application_id:d?.passport_application_id,payment_obligation_amount:d?.payment_obligation_amount,payment_source:d?.payment_source}},EXP.A.passport),src=commercialSource('A',EXP.A.passport);console.log('PASSPORT_SOURCE_TO_PROJECTION_PROOF',JSON.stringify({source:{relation:SOURCE_MAP.passport.relation,amount:src.obligation_amount,currency:src.currency,application_id:src.application_id},projection}));if(Number(projection.amount)!==EXP.A.amount||upper(projection.currency)!==EXP.A.currency||projection.source!=='FINALIZED_APPLICATION_COMMERCIAL_TERMS'||projection.application_id!==EXP.A.source_application_id||projection.payment_obligation_amount!==null||projection.payment_source!=='DEAL_FINANCE_STATUS')fail(failures,'PASSPORT_SOURCE_TO_PROJECTION',{projection});const counts=await page.evaluate(ids=>Object.fromEntries(ids.map(id=>[id,[...document.querySelectorAll(`[data-open-deal="${id}"]`)].filter(x=>(x.offsetWidth||x.offsetHeight)&&x.closest('[data-rona-deals-authoritative-list]')).length])),[...EXP.A.active_ids,...EXP.A.terminal_ids]);const dproof={counts,active_ids:EXP.A.active_ids,terminal_ids:EXP.A.terminal_ids};console.log('REAL_DEAL_ROW_COUNTS',JSON.stringify(dproof));if(EXP.A.active_ids.some(id=>counts[id]!==1)||EXP.A.terminal_ids.some(id=>counts[id]!==0))fail(failures,'DEALS_ONE_VISIBLE_ROW_PER_ID',dproof);await page.evaluate(id=>document.querySelector(`[data-rona-deals-authoritative-list] [data-open-deal="${id}"]`)?.click(),EXP.A.passport);await wait(page,id=>document.querySelector(`.rona-deal-command-center-v3[data-rona-authoritative-deal-id="${id}"]`),EXP.A.passport,3000);await wait(page,()=>/resource fixture status|Подтверждение ресурса/iu.test(document.querySelector('#rona-deal-realization-flow-v3')?.innerText||''),null,3000);const pp=await page.evaluate(()=>{const nrm=v=>String(v??'').replace(/\s+/g,' ').trim(),d=document.querySelector('.rona-deal-command-center-v3[data-rona-authoritative-binding="authoritative-binding"]');if(!d)return null;const a=[...d.querySelectorAll('.pair,[data-rona-command-field]')].map(x=>nrm(x.innerText)).find(x=>/Сумма/i.test(x))||'',token=a.replace(/[^0-9,.-]/g,''),numeric=Number(token.includes(',')?token.replace(/\./g,'').replace(',','.'):token),flow=d.querySelector('#rona-deal-realization-flow-v3');return{client:d.dataset.ronaAuthoritativeClientId,contract:d.dataset.ronaAuthoritativeContractId,deal:d.dataset.ronaAuthoritativeDealId,amount:numeric,currency:(a.match(/USD|EUR|RUB|KGS/iu)||[])[0]||'',owner:flow?.dataset?.ronaRealizationOwner||'',status:nrm(flow?.innerText)}});console.log('PRODUCTION_PASSPORT_LINEAGE_PROOF',JSON.stringify({source:{relation:SOURCE_MAP.passport.relation,amount:src.obligation_amount,currency:src.currency,application_id:src.application_id},projection,visible:pp}));if(!pp||pp.client!==EXP.A.client||pp.contract!==EXP.A.contract||pp.deal!==EXP.A.passport||Math.abs(pp.amount-EXP.A.amount)>.01||upper(pp.currency)!==EXP.A.currency||!/server-authoritative/i.test(pp.owner)||!/resource fixture status|Подтверждение ресурса/iu.test(pp.status))fail(failures,'PASSPORT_AMOUNT_AND_CONTEXT',pp||{});
if(base){const passportTypography=await ownerTypoScope(page,'.rona-deal-command-center-v3[data-rona-authoritative-binding="authoritative-binding"]');const sectionTypography={};for(const sec of ['home','companies','prices','applications','deals'])sectionTypography[sec]=await ownerTypoSection(page,sec);const navTypography=await ownerTypoScope(page,'#nav');const bp=await ctx.newPage();await bp.goto(base.origin+'/portal/client',{waitUntil:'domcontentloaded'});await sleep(600);const ba=await typeSample(bp,'analytics'),ta=await typeSample(page,'analytics'),analyticsRatios=['nav','title','sub','leaf'].filter(k=>Number.isFinite(ba[k])&&Number.isFinite(ta[k])&&ba[k]>0).map(k=>({key:k,ratio:ta[k]/ba[k],base:ba[k],target:ta[k]})),analyticsMarkers=await page.evaluate(()=>[...document.querySelectorAll('#page-analytics [data-rona-owner-typo-scaled],#analyticsPage [data-rona-owner-typo-scaled],[data-page-panel="analytics"] [data-rona-owner-typo-scaled],[data-page-id="analytics"] [data-rona-owner-typo-scaled]')].length),proof={marker:document.documentElement?.dataset?.ronaClientOwnerTypography,nav:navTypography,sections:sectionTypography,passport:passportTypography,analytics:{base:ba,target:ta,ratios:analyticsRatios,markers:analyticsMarkers}};console.log('REAL_TYPOGRAPHY_PROOF',JSON.stringify(proof));const badSections=Object.entries(sectionTypography).filter(([,v])=>!ownerTypoScopePass(v,3)).map(([k,v])=>({section:k,eligible:v.eligible,marked:v.marked,bad:v.bad,clipped:v.clipped,marker:v.html_marker})),analyticsBad=analyticsMarkers!==0||analyticsRatios.length<2||analyticsRatios.some(x=>x.ratio<.98||x.ratio>1.02);if(!ownerTypoScopePass(navTypography,3)||!ownerTypoScopePass(passportTypography,3)||badSections.length||analyticsBad)fail(failures,'CLIENT_TYPOGRAPHY_REAL_UI_110',{nav:{eligible:navTypography.eligible,marked:navTypography.marked,bad:navTypography.bad,clipped:navTypography.clipped,marker:navTypography.html_marker},passport:{eligible:passportTypography.eligible,marked:passportTypography.marked,bad:passportTypography.bad,clipped:passportTypography.clipped,marker:passportTypography.html_marker},badSections,analytics:{ratios:analyticsRatios,markers:analyticsMarkers}});await bp.close()}
const sw=requests.length;await page.evaluate(({a,b})=>{window.RONA_CLIENT_CONTEXT.select(b.client_id,b.contract_id);setTimeout(()=>window.RONA_CLIENT_CONTEXT.select(a.client_id,a.contract_id),15)},{a:FIX.A,b:FIX.B});const recovered=await wait(page,id=>window.RONA_CLIENT_CONTEXT?.getCurrentContext?.()?.client_id===id&&window.RONA_CLIENT_CONTEXT?.getCurrentProjection?.()?.contract?.client_id===id,FIX.A.client_id,4000);await sleep(250);const visible=await page.evaluate(()=>String(document.querySelector('main')?.innerText||'').replace(/\s+/g,' ').trim()),foreign=[FIX.B.client_id,FIX.B.contract_id,FIX.B.legal,FIX.B.external].some(value=>visible.includes(value)),switchReq=requests.slice(sw).filter(x=>x.label===LABEL&&x.path==='/portal/api/v1/client/context');console.log('MULTI_CONTEXT_STALE_COMMIT_PROOF',JSON.stringify({recovered,foreign,requests:switchReq}));if(!recovered||foreign)fail(failures,'MULTI_CONTEXT_STALE_COMMIT',{recovered,foreign});const before=requests.filter(x=>x.label===LABEL&&x.path==='/portal/api/v1/client/context').length;await page.evaluate(()=>{window.dispatchEvent(new Event('focus'));window.dispatchEvent(new Event('pageshow'));document.dispatchEvent(new Event('visibilitychange'));const p=document.body.appendChild(document.createElement('div'));for(let i=0;i<30;i++){const n=p.appendChild(document.createElement('span'));n.remove()}p.remove()});await sleep(1200);const after=requests.filter(x=>x.label===LABEL&&x.path==='/portal/api/v1/client/context').length;console.log('NO_FANOUT_IDLE_STABILITY_PROOF',JSON.stringify({before,after,delta:after-before,wait_ms:1200}));if(after!==before)fail(failures,'FOCUS_PAGESHOW_VISIBILITY_OBSERVER_FANOUT',{before,after});if(failures.length){console.log('OWNER_RETEST_REGRESSION_RESULT=FAIL',JSON.stringify({label:LABEL,failures}));process.exitCode=1}else{console.log('OWNER_RETEST_ASSERTION_SET=PASS',JSON.stringify({label:LABEL,assertions:['home-reentry-ready-within-300ms','typography-110-analytics-100-no-clipping','production-semantic-company-kpis-mixed-status','one-visible-row-per-active-authoritative-deal-terminal-hidden','canonical-finalized-application-source-to-api-projection-to-passport','exact-passport-binding-lifecycle-status','multi-context-no-stale-foreign','focus-pageshow-visibility-no-fanout','observer-idle-network-stable']}));console.log('OWNER_RETEST_REGRESSION_RESULT=PASS')}}finally{if(browser)await browser.close();await stop(target.server);if(base)await stop(base.server)}
