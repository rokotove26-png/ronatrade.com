import { readFile, writeFile } from 'node:fs/promises';
import { createHash } from 'node:crypto';
const htmlPath='dist/portal/client.html';
const integrityPath='dist/canonical-visual-integrity.json';
const approvalPath='governance/client-portal-visual-freeze.json';
const sourceRuntimePath='assets/portal-runtime/portal-client-applications-canonical-v1.js';
const runtimePath='dist/assets/portal-runtime/portal-client-applications-canonical-v1.js';
const id='rona-portal-client-applications-canonical-v1';
const sourceMarker='20260909-portal-client-applications-canonical-v4-single-presentation-owner';
const marker='20260908-portal-client-applications-canonical-v4-current-projection';
const counterOfferMarker='20260910-client-counter-offer-canonical-emitted-v1';
const sha256=b=>createHash('sha256').update(b).digest('hex');
const approval=JSON.parse(await readFile(approvalPath,'utf8'));
if(approval?.client_applications_live_render_correction?.authorized_source!=='OWNER_IN_CHAT')throw new Error('CLIENT_APPLICATIONS_OWNER_APPROVAL_MISSING');
const sourceRuntime=await readFile(sourceRuntimePath,'utf8');
for(const required of [sourceMarker,'applications-projection','application_price','resource_status','Цена заявки','Подтверждение ресурса','applications-title-frame','contentLeft','data-rona-live-applications="canonical-v1"','rona-live-app-summary','rona-live-app-terms','rona-live-app-state-strip','grid-template-columns:minmax(0,1fr) auto auto','flex-wrap:nowrap','font-size:14px','font-size:13.5px','font:740 12.2px/1'])if(!sourceRuntime.includes(required))throw new Error(`CLIENT_APPLICATIONS_CANONICAL_REQUIRED_MISSING: ${required}`);
for(const forbidden of ['ACCEPT_PUBLISHED_PRICE','price_mode','Режим цены','portal-client-applications-uat-v2','portal-client-applications-uat-v3','rona-live-app-state-lines'])if(sourceRuntime.includes(forbidden))throw new Error(`CLIENT_APPLICATIONS_CANONICAL_FORBIDDEN_OUTPUT: ${forbidden}`);
if(/<img|<svg|<canvas|background-image\s*:/iu.test(sourceRuntime))throw new Error('CLIENT_APPLICATIONS_CANONICAL_IMAGE_ASSET_FORBIDDEN');
const ghost="const detail=await request('/v1/client/applications-projection?clientId='+encodeURIComponent(norm(ctx.client_id))+'&contractId='+encodeURIComponent(norm(ctx.contract_id)));";
const live="const a=authority();if(!a?.whenCurrentProjection)throw new Error('CLIENT_CONTEXT_AUTHORITY_UNAVAILABLE');const detail=await a.whenCurrentProjection('applications-canonical');";
const oldSubmit="window.addEventListener('rona:client-application-submitted',()=>{setTimeout(()=>load(true),120);setTimeout(()=>load(true),900)})";
const newSubmit="window.addEventListener('rona:client-application-submitted',()=>{authority()?.invalidateCurrentProjection?.();setTimeout(()=>load(true),120);setTimeout(()=>{authority()?.invalidateCurrentProjection?.();load(true)},900)})";
if(!sourceRuntime.includes(ghost))throw new Error('CLIENT_APPLICATIONS_GHOST_ENDPOINT_TARGET_MISSING');
if(!sourceRuntime.includes(oldSubmit))throw new Error('CLIENT_APPLICATIONS_SUBMIT_INVALIDATION_TARGET_MISSING');
let runtime=sourceRuntime.replace(`const MARK='${sourceMarker}';`,`const MARK='${marker}';`).replace(ghost,live).replace(oldSubmit,newSubmit);
if(runtime.includes('applications-projection'))throw new Error('CLIENT_APPLICATIONS_GHOST_ENDPOINT_NOT_REMOVED');
for(const required of [marker,'whenCurrentProjection','invalidateCurrentProjection','state.timer=setInterval(()=>load(false),REFRESH_MS)',"window.addEventListener('pageshow',()=>{load(true)"])if(!runtime.includes(required))throw new Error(`CLIENT_APPLICATIONS_CURRENT_PROJECTION_CONTRACT_MISSING: ${required}`);

const counterOfferRuntime=String.raw`
;(()=>{'use strict';
const MARK='${counterOfferMarker}';
if(window.__RONA_CLIENT_COUNTER_OFFER_CANONICAL__===MARK)return;
window.__RONA_CLIENT_COUNTER_OFFER_CANONICAL__=MARK;
if(!/^\/portal\/client\/?$/.test(location.pathname))return;
const norm=value=>String(value??'').trim();
const num=value=>{const n=Number(value);return Number.isFinite(n)?n:null};
const fmt=value=>{const n=num(value);return n===null?'—':new Intl.NumberFormat('ru-RU',{maximumFractionDigits:3}).format(n)};
const authority=()=>window.RONA_CLIENT_CONTEXT||null;
let decorateToken=0;
function ensureCounterStyle(){if(document.getElementById('rona-client-counter-offer-canonical-style'))return;const style=document.createElement('style');style.id='rona-client-counter-offer-canonical-style';style.textContent='#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-panel{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;padding:10px 12px;border:1px solid rgba(93,180,226,.24);border-radius:9px;background:rgba(10,42,63,.54);color:rgba(232,243,250,.96)}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-copy{display:flex;align-items:baseline;gap:8px;flex-wrap:wrap;min-width:0;font:680 12.4px/1.35 Inter,system-ui,sans-serif}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-price{color:rgba(225,248,238,.98);font-weight:820;font-size:13.6px;white-space:nowrap}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-state{color:rgba(205,222,234,.82);font-size:11.8px}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-action{min-height:30px;padding:0 12px;border:1px solid rgba(93,180,226,.34);border-radius:8px;background:linear-gradient(180deg,rgba(16,58,86,.86),rgba(7,34,54,.90));color:rgba(241,248,252,.96);font:760 11.8px/1 Inter,system-ui,sans-serif;cursor:pointer}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-action[data-decision="accept"]{border-color:rgba(90,187,161,.34);background:rgba(29,93,77,.34)}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-action:disabled{opacity:.55;cursor:wait}#page-applications [data-rona-live-applications="canonical-v1"] .rona-counter-offer-error{width:100%;color:#f2b7b7;font:650 11.5px/1.35 Inter,system-ui,sans-serif}';document.head.appendChild(style)}
function offerPrice(app){const price=num(app?.counter_price),currency=norm(app?.counter_currency).toUpperCase();return price===null||!currency?'':fmt(price)+' '+currency+'/т'}
function responseLabel(app){const response=norm(app?.client_counter_response).toUpperCase();if(response==='ACCEPTED')return'Встречное предложение принято';if(response==='DECLINED')return'Встречное предложение отклонено';return''}
function makePanel(app){const price=offerPrice(app),active=app?.counter_offer_active===true,response=responseLabel(app);if(!price||(!active&&!response))return null;const panel=document.createElement('div');panel.className='rona-counter-offer-panel';panel.dataset.ronaCounterOfferPanel='v1';panel.dataset.ronaCounterOfferActive=String(active);const copy=document.createElement('div');copy.className='rona-counter-offer-copy';const label=document.createElement('span');label.textContent='Встречное предложение RONA Trade';const amount=document.createElement('strong');amount.className='rona-counter-offer-price';amount.setAttribute('data-rona-counter-offer-price','true');amount.textContent=price;copy.append(label,amount);if(response){const state=document.createElement('span');state.className='rona-counter-offer-state';state.textContent=response;copy.append(state)}panel.append(copy);if(active){const actions=document.createElement('div');actions.className='rona-counter-offer-actions';for(const pair of [['accept','Принять'],['decline','Отклонить']]){const button=document.createElement('button');button.type='button';button.className='rona-counter-offer-action';button.dataset.ronaCounterOfferDecision=pair[0];button.dataset.applicationId=norm(app?.application_id);button.dataset.decision=pair[0];button.textContent=pair[1];actions.append(button)}panel.append(actions)}return panel}
async function decorate(){const token=++decorateToken,a=authority();if(!a?.whenCurrentProjection)return;let projection;try{projection=await a.whenCurrentProjection('client-counter-offer-canonical')}catch{return}if(token!==decorateToken)return;const apps=Array.isArray(projection?.applications)?projection.applications:[],byId=new Map(apps.map(app=>[norm(app?.application_id),app]).filter(pair=>pair[0]));const root=document.querySelector('#page-applications [data-rona-live-applications="canonical-v1"]');if(!root)return;ensureCounterStyle();for(const row of root.querySelectorAll('[data-rona-live-application-id]')){row.querySelector('[data-rona-counter-offer-panel]')?.remove();const app=byId.get(norm(row.getAttribute('data-rona-live-application-id')));if(!app)continue;const panel=makePanel(app);if(panel){const summary=row.querySelector('.rona-live-app-summary');(summary||row).append(panel)}}}
function queueDecorate(){setTimeout(()=>decorate(),0)}
async function decide(button){const id=norm(button?.dataset?.applicationId),decision=norm(button?.dataset?.ronaCounterOfferDecision);if(!id||!['accept','decline'].includes(decision))return;const panel=button.closest('[data-rona-counter-offer-panel]'),buttons=[...(panel?.querySelectorAll('button')||[])];buttons.forEach(x=>x.disabled=true);panel?.querySelector('.rona-counter-offer-error')?.remove();try{const backendPath='/client/applications/'+encodeURIComponent(id)+'/counter-offer/'+decision;const response=await fetch('/portal/owner-api?path='+encodeURIComponent(backendPath),{method:'POST',credentials:'same-origin',cache:'no-store',headers:{accept:'application/json','content-type':'application/json'},body:'{}'});const payload=await response.json().catch(()=>null);if(!response.ok||payload?.ok===false)throw new Error(String(payload?.code||('HTTP_'+response.status)));authority()?.invalidateCurrentProjection?.();window.dispatchEvent(new CustomEvent('rona:client-application-submitted',{detail:{source:'COUNTER_OFFER_RESPONSE'}}));setTimeout(queueDecorate,180);setTimeout(queueDecorate,980)}catch(error){buttons.forEach(x=>x.disabled=false);if(panel){const message=document.createElement('div');message.className='rona-counter-offer-error';message.textContent='Не удалось сохранить ответ. Обновите данные и повторите действие.';panel.append(message)}console.error('RONA client counter-offer response',error)}}
document.addEventListener('click',event=>{const button=event.target?.closest?.('[data-rona-counter-offer-decision]');if(button)decide(button)},true);
window.addEventListener('rona:client-applications-rendered',queueDecorate);
window.addEventListener('rona:client-application-submitted',queueDecorate);
window.addEventListener('pageshow',queueDecorate,{passive:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',queueDecorate,{once:true});else queueDecorate();
})();`;
runtime+=counterOfferRuntime;
for(const required of [counterOfferMarker,'counter_offer_active','counter_price','counter_currency','client_counter_response','data-rona-counter-offer-price','Принять','Отклонить','/counter-offer/','/portal/owner-api?path=','invalidateCurrentProjection','rona:client-application-submitted'])if(!runtime.includes(required))throw new Error(`CLIENT_COUNTER_OFFER_CANONICAL_EMIT_MISSING: ${required}`);

await writeFile(runtimePath,runtime,'utf8');
let html=await readFile(htmlPath,'utf8');
for(const competing of ['rona-client-applications-live-render-v1','rona-client-applications-live-render-v2','rona-portal-client-applications-uat-v2','rona-portal-client-applications-uat-v3','client-applications-canonical-layout-v1.js'])if(html.includes(competing))throw new Error(`CLIENT_APPLICATIONS_COMPETING_RENDERER_PRESENT: ${competing}`);
if(html.includes(id)||html.includes('portal-client-applications-canonical-v1.js'))throw new Error('CLIENT_APPLICATIONS_CANONICAL_ALREADY_PRESENT');
const digest=sha256(Buffer.from(runtime,'utf8')).slice(0,16),src=`/assets/portal-runtime/portal-client-applications-canonical-v1.js?v=${digest}`;
const close=html.toLowerCase().lastIndexOf('</body>');if(close<0)throw new Error('CLIENT_BODY_CLOSE_MISSING');
html=html.slice(0,close)+`<script id="${id}" src="${src}" defer></script>`+html.slice(close);
if((html.match(/portal-client-applications-canonical-v1\.js/g)||[]).length!==1)throw new Error('CLIENT_APPLICATIONS_CANONICAL_NOT_SINGLE_OWNER');
await writeFile(htmlPath,html,'utf8');
const integrity=JSON.parse(await readFile(integrityPath,'utf8')),emitted=Buffer.from(html,'utf8');integrity.client_runtime.emitted_sha256=sha256(emitted);integrity.client_runtime.emitted_bytes=emitted.length;integrity.client_runtime.applications_live_render={id,src,marker,ownership:'SINGLE_RENDERER',source:'RONA_CLIENT_CONTEXT_CURRENT_PROJECTION',endpoint:'/portal/api/v1/client/context',alignment:'APPLICATIONS_TITLE_FRAME',alignment_box_model:'CONTENT_BOX_AWARE',price:'IMMUTABLE_SUBMITTED_APPLICATION_PRICE',resource_status:['RESOURCE_NOT_CONFIRMED','RESOURCE_CONFIRMED'],internal_price_mode_visible:false,visual_context:'DEAL_CARD_VISUAL_PARITY',layout:'HEADLINE_PRICE_ACTION_TERMS_STATE_STRIP',status_layout:'SINGLE_LINE_INDICATOR_STRIP',typography:'DEAL_CARD_SCALE',competing_renderers:false,refresh_policy:'INITIAL_CONTEXT_CHANGE_SUBMIT_PAGESHOW_TTL',periodic_refresh:true,pageshow_network_refresh:true,post_submit_projection_invalidation:true,ghost_endpoint:false,images_added:false,counter_offer:{marker:counterOfferMarker,source_fields:['counter_price','counter_currency','client_counter_response','counter_offer_active'],actions:['accept','decline'],mutation_path:'/portal/owner-api -> /client/applications/{application_id}/counter-offer/{accept|decline}',post_response_projection_invalidation:true,single_emitted_runtime:true}};await writeFile(integrityPath,JSON.stringify(integrity,null,2)+'\n','utf8');
console.log(`CLIENT_APPLICATIONS_CANONICAL=PASS marker=${marker} counter_offer=${counterOfferMarker} data=RONA_CLIENT_CONTEXT_CURRENT_PROJECTION ghost-endpoint=false ttl=true submit-invalidation=true single-renderer=true`);
