const base=String(process.env.TARGET_ORIGIN||'https://ronaoil.com').replace(/\/$/,'');
const sha=process.env.GITHUB_SHA||Date.now();
const assert=(v,m)=>{if(!v)throw new Error(m)};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function retry(label,fn,attempts=30,delayMs=3000){let last;for(let i=1;i<=attempts;i++){try{return await fn(i)}catch(e){last=e;if(i<attempts)await sleep(delayMs)}}throw new Error(`${label}: ${last?.message||last}`)}
const fetchNoStore=(path,attempt=0,opts={})=>fetch(`${base}${path}${path.includes('?')?'&':'?'}_qa=${encodeURIComponent(sha)}&_attempt=${attempt}&_nonce=${Date.now()}`,{cache:'no-store',...opts});

const integrity=await retry('integrity',async attempt=>{const r=await fetchNoStore('/canonical-visual-integrity.json',attempt);assert(r.ok,`status ${r.status}`);const x=await r.json();assert(x.architecture==='CURRENT_ONLY_ADMIN_SHELL_WITH_FROZEN_CANONICAL_ASSETS',`architecture ${x.architecture}`);assert(x.admin_runtime?.state==='CURRENT_ONLY','Admin state is not CURRENT_ONLY');assert(x.admin_runtime?.legacy_runtime_in_deployment===false,'legacy Admin runtime is deployed');assert(Number(x.admin_runtime?.emitted_bytes||0)>0&&Number(x.admin_runtime?.emitted_bytes||0)<60000,'Admin shell size contract failed');return x});

await retry('Admin auth shell',async attempt=>{const r=await fetchNoStore('/portal/admin',attempt,{redirect:'manual'});assert(r.status===302,`status ${r.status}`);const location=new URL(r.headers.get('location'),base);assert(location.pathname==='/portal/login'&&location.searchParams.get('next')==='/portal/admin',`redirect ${location.href}`);assert(r.headers.get('x-rona-admin-shell')==='current-only-v2',`shell ${r.headers.get('x-rona-admin-shell')}`);assert(r.headers.get('x-rona-admin-auth')==='server-verified-v1','server auth marker missing');assert(r.headers.get('x-rona-admin-current-only')==='main-v2-shell-v2','current-only marker missing');return true});

const main=await retry('main-v2 semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/main-ui',attempt);assert(r.ok&&r.headers.get('x-rona-ui')==='main-v2',`status ${r.status} ui ${r.headers.get('x-rona-ui')}`);const t=await r.text();assert(t.length>0,'main-v2 body empty');return t});
const runtimeSrc=await retry('single-owner runtime semantic convergence',async attempt=>{const r=await fetchNoStore('/assets/portal-admin-shell-fast-v1.js',attempt);assert(r.ok,`status ${r.status}`);const t=await r.text();assert(t.includes("window.__RONA_ADMIN_SHELL_RESILIENCE__='single-owner-v3'"),'single-owner marker missing');assert(t.includes('/portal/main-ui'),'single-owner runtime does not load main-v2');assert(t.includes('/portal/claims-r2-ui'),'single-owner runtime does not load Claims');assert(t.includes('/portal/remaining-sections-ui'),'single-owner runtime does not load Agent Rewards/remaining sections');assert(t.includes('/portal/analytics-v2-ui'),'single-owner runtime does not load Analytics');for(const forbidden of ['clients-agents-v4-ui','clients-agents-canonical-guard-ui','remaining-sections-final-polish-ui','remaining-sections-functional-preserve-v2-ui','owner-layout-polish-ui','admin-access-ui','title-visual-rollback-ui','claims-title-hotfix'])assert(!t.includes(forbidden),`competing Admin runtime returned: ${forbidden}`);return t});
const watchdogSrc=await retry('page-aware watchdog semantic convergence',async attempt=>{const r=await fetchNoStore('/assets/portal-admin-runtime-watchdog-v1.js',attempt);assert(r.ok,`status ${r.status}`);const t=await r.text();assert(t.includes("window.__RONA_ADMIN_RUNTIME_WATCHDOG__='page-aware-v7-analytics-rendered-ready'"),'page-aware-v7 marker missing');assert(t.includes("if(p==='analytics')return !!n.querySelector('#rona-analytics-v2 .an2-head')"),'Analytics rendered-ready marker missing');assert(t.includes("if(p==='market-news')return marketNewsReady()"),'market-news owner marker missing');assert(t.includes("root.querySelector(':scope > .mn-masthead')"),'market-news health marker missing');assert(!t.includes('location.reload(')&&!t.includes('location.replace('),'destructive recovery returned');return t});
const access=await retry('Clients/Agents static semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/clients-agents-current-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-delivery')==='static-build-v1',`delivery ${r.headers.get('x-rona-delivery')}`);assert(r.headers.get('x-rona-clients-agents-ui')==='single-owner-v4',`owner ${r.headers.get('x-rona-clients-agents-ui')}`);assert(r.headers.get('x-rona-access-create')==='client-agent-v4',`create ${r.headers.get('x-rona-access-create')}`);assert(r.headers.get('x-rona-admin-nav-owner')==='external-current-router-v2',`nav ${r.headers.get('x-rona-admin-nav-owner')}`);assert(r.headers.get('x-rona-shell-mutation')==='none','page mutates global shell');assert(r.headers.get('x-rona-legacy-dependency')==='none','legacy dependency returned');const t=await r.text();for(const marker of ["window.__RONA_CLIENTS_AGENTS_CURRENT__='20260826-single-owner-v4'",'ronaCreateAccess','admin-authority','Тип доступа','Договоры клиента','Профиль агента','Роль пользователя','openWithoutContract','История и права','Сменить пароль','Будет создан клиентский доступ по выбранным договорам.','Будет создан доступ в кабинет агента.','Создать доступ'])assert(t.includes(marker),`missing static access semantic: ${marker}`);for(const legacy of ['harvestLegacy','rona-admin-auth-v3413','installNavigationStability','installShellParity'])assert(!t.includes(legacy),`legacy/competing dependency ${legacy}`);return t});
const claims=await retry('Claims static semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/claims-r2-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-delivery')==='static-build-v1',`delivery ${r.headers.get('x-rona-delivery')}`);const t=await r.text();assert(t.includes('#page-claims')&&t.includes('__RONA_CLAIMS_R2_UI__'),'claims contract missing');assert(t.includes('Зарегистрировать и направить клиенту'),'Claims workflow action missing');return t});
const remaining=await retry('Agent Rewards static semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/remaining-sections-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-delivery')==='static-build-v2-no-analytics-no-news',`delivery ${r.headers.get('x-rona-delivery')}`);const t=await r.text();assert(t.includes('renderRewards'),'agent rewards renderer missing');assert(t.includes('Вознаграждения агентов'),'Agent Rewards title missing');assert(t.includes('Радиорубка'),'Radio title missing');return t});
const analytics=await retry('Analytics canonical v4.3.2 semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/analytics-v2-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-delivery')==='static-build-canonical-v432-single-owner',`delivery ${r.headers.get('x-rona-delivery')}`);assert(r.headers.get('x-rona-analytics-ui')==='approved-v4.3.2-pricing-bridge-single-owner',`analytics ui ${r.headers.get('x-rona-analytics-ui')}`);assert(r.headers.get('x-rona-analytics-owner')==='approved-v432-exclusive',`analytics owner ${r.headers.get('x-rona-analytics-owner')}`);assert(r.headers.get('x-rona-analytics-source-file')==='RONA_Admin_LK_LOCAL_v4_3_2_Analytics_PricingBridge_Ready_Local.html',`analytics source ${r.headers.get('x-rona-analytics-source-file')}`);assert(r.headers.get('x-rona-analytics-layout')==='approved-hero-balanced-1520-v432',`analytics layout ${r.headers.get('x-rona-analytics-layout')}`);assert(r.headers.get('x-rona-analytics-visual')==='approved-hero-v432-pricing-bridge',`analytics visual ${r.headers.get('x-rona-analytics-visual')}`);assert(r.headers.get('x-rona-analytics-chart')==='designer-v3-shared-gasoline-axis',`analytics chart ${r.headers.get('x-rona-analytics-chart')}`);const t=await r.text();for(const marker of ['approved-v4.3.2-pricing-bridge-single-owner','RONA_Admin_LK_LOCAL_v4_3_2_Analytics_PricingBridge_Ready_Local.html','RONA TRADE · ANALYTICS','АИ-92','АИ-95','ДТ','LPG / СУГ','Platts LPG / СУГ','Petromarket · DAP benchmark','1075.25','1226.75','DATA-DRIVEN','FACT / CALCULATION / FORECAST','Аналитический вывод'])assert(t.includes(marker),`Analytics marker missing: ${marker}`);for(const stale of ['rona-analytics-canonical-title','ronaAnalyticsCanonicalHomeVisualV1','LPG Hairatan','Комментарий Коммерческого директора','Аналитическая лента'])assert(!t.includes(stale),`stale Analytics marker returned: ${stale}`);return t});
const approvedShell=await retry('Approved shell v4.5.5 semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/admin-approved-shell-v455-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-admin-shell-visual')==='approved-v4.5.5',`visual ${r.headers.get('x-rona-admin-shell-visual')}`);const t=await r.text();for(const marker of ['RONA_NAV_ATTENTION','rona-topbar-premium','ronaAuroraNight','/assets/portal-canonical/background.png'])assert(t.includes(marker),`approved shell marker missing: ${marker}`);return t});
const approvedClaims=await retry('Approved Claims v4.5.5 semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/admin-approved-claims-v455-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-claims-visual')==='approved-v4.5.5',`visual ${r.headers.get('x-rona-claims-visual')}`);const t=await r.text();assert(t.includes('ronaClaimsV455Style')&&t.includes('1140px')&&t.includes('372px'),'approved Claims geometry missing');return t});
const retiredAnalytics=await retry('Retired Analytics v4.5.5 compatibility guard semantic convergence',async attempt=>{const r=await fetchNoStore('/portal/admin-approved-analytics-v455-ui',attempt);assert(r.ok,`status ${r.status}`);assert(r.headers.get('x-rona-analytics-approved')==='retired-compat-guard-v1',`compat guard ${r.headers.get('x-rona-analytics-approved')}`);const t=await r.text();assert(t.includes("__RONA_ANALYTICS_APPROVED_V455__==='retired-compat-guard-v1'"),'retired Analytics compatibility guard marker missing');for(const stale of ['A92.map(x=>x+40)','Platts propane · CIF NWE','прогнозный рыночный нетбек'])assert(!t.includes(stale),`retired Analytics renderer still active: ${stale}`);return t});
await retry('canonical visual assets semantic convergence',async attempt=>{const [bg,logo]=await Promise.all([fetchNoStore('/assets/portal-canonical/background.png',attempt),fetchNoStore('/assets/portal-canonical/logo.svg',attempt)]);assert(bg.ok&&logo.ok,`bg ${bg.status} logo ${logo.status}`);return true});

assert(main.length>0&&runtimeSrc.length>0&&watchdogSrc.length>0&&access.length>0&&claims.length>0&&remaining.length>0&&analytics.length>0&&approvedShell.length>0&&approvedClaims.length>0&&retiredAnalytics.length>0,'semantic proof body unexpectedly empty');
console.log('ADMIN_CURRENT_ONLY_CUSTOM_DOMAIN_STATIC_SEMANTIC_CONVERGENCE=PASS',JSON.stringify({base,sha,adminState:integrity.admin_runtime.state,adminBytes:integrity.admin_runtime.emitted_bytes,legacyRuntime:false,shell:'current-only-v2',runtime:'single-owner-v3',watchdog:'page-aware-v7-analytics-rendered-ready',access:'client-agent-v4',claims:true,rewards:true,analytics:'approved-v4.3.2-pricing-bridge-single-owner',analyticsOwner:'approved-v432-exclusive',analyticsSource:'RONA_Admin_LK_LOCAL_v4_3_2_Analytics_PricingBridge_Ready_Local.html',analyticsChart:'designer-v3-shared-gasoline-axis',analyticsCompatGuard:'retired-compat-guard-v1',approvedVisual:'v4.5.5',delivery:'current-static-modules',nav:'current-only-router-v2',semanticRetryAttempts:30,semanticRetryDelayMs:3000}));
